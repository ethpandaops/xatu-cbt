name: test-mainnet
on:
  pull_request:
  workflow_dispatch:
    inputs:
      xatu_ref:
        description: 'Xatu repository ref to checkout'
        required: false
        default: 'master'
        type: string
permissions:
  contents: read
jobs:
  detect:
    name: Detect impacted models
    runs-on: ubuntu-latest
    outputs:
      models: ${{ steps.detect.outputs.models }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
      - name: Detect impacted models
        id: detect
        run: |
          IMPACTED=$(./.github/scripts/detect_impacted_models.sh --network mainnet --base origin/${{ github.base_ref || 'master' }})
          echo "models=$IMPACTED" >> "$GITHUB_OUTPUT"
          echo "Impacted models: $IMPACTED"
  run-tests:
    name: Test mainnet
    needs: detect
    if: needs.detect.outputs.models != 'none'
    runs-on:
      - self-hosted-ghr
      - size-l-x64
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Set up env
        run: |
          cp example.env .env
      - name: Checkout Xatu
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          repository: ethpandaops/xatu
          ref: ${{ github.event.inputs.xatu_ref || 'master' }}
          path: xatu
      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: '1.24'
      - name: Log in to Docker Hub
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build xatu-cbt
        run: make build
      - name: Create cache directory
        run: mkdir -p .parquet_cache
      - name: Start infrastructure
        id: start-infra
        run: ./bin/xatu-cbt infra start
      - name: Run mainnet tests (full suite)
        if: needs.detect.outputs.models == 'all'
        run: ./bin/xatu-cbt test all --network mainnet --verbose --concurrency 10
      - name: Run mainnet tests (impacted only)
        if: needs.detect.outputs.models != 'all'
        run: |
          echo "Running impacted models: ${{ needs.detect.outputs.models }}"
          ./bin/xatu-cbt test models ${{ needs.detect.outputs.models }} --network mainnet --verbose --concurrency 10
      - name: Stop infrastructure
        if: always() && steps.start-infra.outcome != 'skipped'
        run: ./bin/xatu-cbt infra stop
