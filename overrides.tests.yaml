# Add testing related overrides here
# docs: https://github.com/ethpandaops/cbt?tab=readme-ov-file#model-overrides
overrides:
  dim_node:
    config:
      schedules:
        forwardfill: "@every 5s"
