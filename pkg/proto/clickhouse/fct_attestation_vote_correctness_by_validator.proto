syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Per-slot attestation vote correctness by validator

message FctAttestationVoteCorrectnessByValidator {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // The slot number
  uint64 slot = 12;
  // The start time of the slot
  uint32 slot_start_date_time = 13;
  // The index of the validator
  uint32 validator_index = 14;
  // Whether the validator attested in this slot
  bool attested = 15;
  // Whether the head vote was correct. NULL if not attested
  google.protobuf.BoolValue head_correct = 16;
  // Whether the target vote was correct. NULL if not attested
  google.protobuf.BoolValue target_correct = 17;
  // Whether the source vote was correct. NULL if not attested
  google.protobuf.BoolValue source_correct = 18;
  // Inclusion distance for the attestation. NULL if not attested
  google.protobuf.UInt32Value inclusion_distance = 19;
}

// Request for listing fct_attestation_vote_correctness_by_validator records
message ListFctAttestationVoteCorrectnessByValidatorRequest {
  // Filter by validator_index - The index of the validator (PRIMARY KEY - required unless using alternatives: slot_start_date_time)
  UInt32Filter validator_index = 1 [(google.api.field_behavior) = OPTIONAL, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by slot_start_date_time - The start time of the slot (ORDER BY column 2 - optional)
  UInt32Filter slot_start_date_time = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 3 [(google.api.field_behavior) = OPTIONAL];
  // Filter by slot - The slot number (optional)
  UInt64Filter slot = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by attested - Whether the validator attested in this slot (optional)
  BoolFilter attested = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by head_correct - Whether the head vote was correct. NULL if not attested (optional)
  NullableBoolFilter head_correct = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by target_correct - Whether the target vote was correct. NULL if not attested (optional)
  NullableBoolFilter target_correct = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by source_correct - Whether the source vote was correct. NULL if not attested (optional)
  NullableBoolFilter source_correct = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by inclusion_distance - Inclusion distance for the attestation. NULL if not attested (optional)
  NullableUInt32Filter inclusion_distance = 9 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_attestation_vote_correctness_by_validator to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 10 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctAttestationVoteCorrectnessByValidator` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 11 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 12 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_attestation_vote_correctness_by_validator records
message ListFctAttestationVoteCorrectnessByValidatorResponse {
  // The list of fct_attestation_vote_correctness_by_validator.
  repeated FctAttestationVoteCorrectnessByValidator fct_attestation_vote_correctness_by_validator = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_attestation_vote_correctness_by_validator record by primary key
message GetFctAttestationVoteCorrectnessByValidatorRequest {
  // The index of the validator
  uint32 validator_index = 1; // Primary key (required)
}

// Response for getting a single fct_attestation_vote_correctness_by_validator record
message GetFctAttestationVoteCorrectnessByValidatorResponse {
  FctAttestationVoteCorrectnessByValidator item = 1;
}

// Query fct_attestation_vote_correctness_by_validator data
service FctAttestationVoteCorrectnessByValidatorService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctAttestationVoteCorrectnessByValidatorRequest) returns (ListFctAttestationVoteCorrectnessByValidatorResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_attestation_vote_correctness_by_validator"
    };
  }
  // Get record | Retrieve a single record by validator_index
  rpc Get(GetFctAttestationVoteCorrectnessByValidatorRequest) returns (GetFctAttestationVoteCorrectnessByValidatorResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_attestation_vote_correctness_by_validator/{validator_index}"
    };
  }
}
