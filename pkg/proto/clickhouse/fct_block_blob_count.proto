syntax = "proto3";

package cbt;

import "common.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Blob count of a block for the finalized chain

message FctBlockBlobCount {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // The slot number
  uint32 slot = 12;
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 13;
  // The epoch number containing the slot
  uint32 epoch = 14;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 15;
  // The beacon block root hash
  string block_root = 16;
  // The number of blobs in the block
  uint32 blob_count = 17;
  // Can be "canonical" or "orphaned"
  string status = 18;
}

// ListFctBlockBlobCountRequest is the request message for listing fct_block_blob_count records
message ListFctBlockBlobCountRequest {
  // Filter by slot_start_date_time (PRIMARY KEY - required)
  UInt32Filter slot_start_date_time = 1;

  // Filter by block_root (ORDER BY column 2 - optional)
  StringFilter block_root = 2;

  // Filter by updated_date_time (optional)
  UInt32Filter updated_date_time = 3;
  // Filter by slot (optional)
  UInt32Filter slot = 4;
  // Filter by epoch (optional)
  UInt32Filter epoch = 5;
  // Filter by epoch_start_date_time (optional)
  UInt32Filter epoch_start_date_time = 6;
  // Filter by blob_count (optional)
  UInt32Filter blob_count = 7;
  // Filter by status (optional)
  StringFilter status = 8;

  // The maximum number of fct_block_blob_count to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 9;
  // A page token, received from a previous `ListFctBlockBlobCount` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 10;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 11;
}

// ListFctBlockBlobCountResponse is the response message for listing fct_block_blob_count records
message ListFctBlockBlobCountResponse {
  // The list of fct_block_blob_count.
  repeated FctBlockBlobCount fct_block_blob_count = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// GetFctBlockBlobCountRequest is the request message for getting a single fct_block_blob_count record by primary key
message GetFctBlockBlobCountRequest {
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 1; // Primary key (required)
}

// GetFctBlockBlobCountResponse is the response message for getting a single fct_block_blob_count record
message GetFctBlockBlobCountResponse {
  FctBlockBlobCount item = 1;
}

// FctBlockBlobCountService provides RPC methods for querying fct_block_blob_count data
service FctBlockBlobCountService {
  // List returns paginated fct_block_blob_count records
  rpc List(ListFctBlockBlobCountRequest) returns (ListFctBlockBlobCountResponse);
  // Get returns a single fct_block_blob_count record by primary key
  rpc Get(GetFctBlockBlobCountRequest) returns (GetFctBlockBlobCountResponse);
}
