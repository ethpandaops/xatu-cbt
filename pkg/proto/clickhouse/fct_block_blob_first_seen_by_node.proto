syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// When the block was first seen on the network by a sentry node

message FctBlockBlobFirstSeenByNode {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Source of the event
  string source = 12;
  // The slot number
  uint32 slot = 13;
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 14;
  // The epoch number containing the slot
  uint32 epoch = 15;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 16;
  // The time from slot start for the client to see the block
  uint32 seen_slot_start_diff = 17;
  // The beacon block root hash
  string block_root = 18;
  // The blob index
  uint32 blob_index = 19;
  // Username of the node
  string username = 20;
  // ID of the node
  string node_id = 21;
  // Classification of the node, e.g. "individual", "corporate", "internal" (aka ethPandaOps) or "unclassified"
  string classification = 22;
  // Name of the client
  string meta_client_name = 23;
  // Version of the client
  string meta_client_version = 24;
  // Implementation of the client
  string meta_client_implementation = 25;
  // City of the client
  string meta_client_geo_city = 26;
  // Country of the client
  string meta_client_geo_country = 27;
  // Country code of the client
  string meta_client_geo_country_code = 28;
  // Continent code of the client
  string meta_client_geo_continent_code = 29;
  // Longitude of the client
  google.protobuf.DoubleValue meta_client_geo_longitude = 30;
  // Latitude of the client
  google.protobuf.DoubleValue meta_client_geo_latitude = 31;
  // Autonomous system number of the client
  google.protobuf.UInt32Value meta_client_geo_autonomous_system_number = 32;
  // Autonomous system organization of the client
  google.protobuf.StringValue meta_client_geo_autonomous_system_organization = 33;
  // Ethereum consensus client version
  string meta_consensus_version = 34;
  // Ethereum consensus client implementation
  string meta_consensus_implementation = 35;
}

// Request for listing fct_block_blob_first_seen_by_node records
message ListFctBlockBlobFirstSeenByNodeRequest {
  // Filter by slot_start_date_time - The wall clock time when the slot started (PRIMARY KEY - required)
  UInt32Filter slot_start_date_time = 1 [(google.api.field_behavior) = REQUIRED];

  // Filter by block_root - The beacon block root hash (ORDER BY column 2 - optional)
  StringFilter block_root = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by blob_index - The blob index (ORDER BY column 3 - optional)
  UInt32Filter blob_index = 3 [(google.api.field_behavior) = OPTIONAL];

  // Filter by meta_client_name - Name of the client (ORDER BY column 4 - optional)
  StringFilter meta_client_name = 4 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by source - Source of the event (optional)
  StringFilter source = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by slot - The slot number (optional)
  UInt32Filter slot = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by epoch - The epoch number containing the slot (optional)
  UInt32Filter epoch = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by seen_slot_start_diff - The time from slot start for the client to see the block (optional)
  UInt32Filter seen_slot_start_diff = 10 [(google.api.field_behavior) = OPTIONAL];
  // Filter by username - Username of the node (optional)
  StringFilter username = 11 [(google.api.field_behavior) = OPTIONAL];
  // Filter by node_id - ID of the node (optional)
  StringFilter node_id = 12 [(google.api.field_behavior) = OPTIONAL];
  // Filter by classification - Classification of the node, e.g. "individual", "corporate", "internal" (aka ethPandaOps) or "unclassified" (optional)
  StringFilter classification = 13 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_client_version - Version of the client (optional)
  StringFilter meta_client_version = 14 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_client_implementation - Implementation of the client (optional)
  StringFilter meta_client_implementation = 15 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_client_geo_city - City of the client (optional)
  StringFilter meta_client_geo_city = 16 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_client_geo_country - Country of the client (optional)
  StringFilter meta_client_geo_country = 17 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_client_geo_country_code - Country code of the client (optional)
  StringFilter meta_client_geo_country_code = 18 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_client_geo_continent_code - Continent code of the client (optional)
  StringFilter meta_client_geo_continent_code = 19 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_client_geo_longitude - Longitude of the client (optional)
  google.protobuf.DoubleValue meta_client_geo_longitude = 20 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_client_geo_latitude - Latitude of the client (optional)
  google.protobuf.DoubleValue meta_client_geo_latitude = 21 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_client_geo_autonomous_system_number - Autonomous system number of the client (optional)
  NullableUInt32Filter meta_client_geo_autonomous_system_number = 22 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_client_geo_autonomous_system_organization - Autonomous system organization of the client (optional)
  NullableStringFilter meta_client_geo_autonomous_system_organization = 23 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_consensus_version - Ethereum consensus client version (optional)
  StringFilter meta_consensus_version = 24 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_consensus_implementation - Ethereum consensus client implementation (optional)
  StringFilter meta_consensus_implementation = 25 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_block_blob_first_seen_by_node to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 26 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctBlockBlobFirstSeenByNode` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 27 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 28 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_block_blob_first_seen_by_node records
message ListFctBlockBlobFirstSeenByNodeResponse {
  // The list of fct_block_blob_first_seen_by_node.
  repeated FctBlockBlobFirstSeenByNode fct_block_blob_first_seen_by_node = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_block_blob_first_seen_by_node record by primary key
message GetFctBlockBlobFirstSeenByNodeRequest {
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 1; // Primary key (required)
}

// Response for getting a single fct_block_blob_first_seen_by_node record
message GetFctBlockBlobFirstSeenByNodeResponse {
  FctBlockBlobFirstSeenByNode item = 1;
}

// Query fct_block_blob_first_seen_by_node data
service FctBlockBlobFirstSeenByNodeService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctBlockBlobFirstSeenByNodeRequest) returns (ListFctBlockBlobFirstSeenByNodeResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_block_blob_first_seen_by_node"
    };
  }
  // Get record | Retrieve a single record by slot_start_date_time
  rpc Get(GetFctBlockBlobFirstSeenByNodeRequest) returns (GetFctBlockBlobFirstSeenByNodeResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_block_blob_first_seen_by_node/{slot_start_date_time}"
    };
  }
}
