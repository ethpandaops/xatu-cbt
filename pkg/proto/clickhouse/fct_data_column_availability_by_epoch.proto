syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Data column availability by epoch and column index for PeerDAS heatmap hour-level drill-down

message FctDataColumnAvailabilityByEpoch {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Epoch number
  uint32 epoch = 12;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 13;
  // Column index (0-127)
  uint64 column_index = 14;
  // Number of slots in this epoch aggregation
  uint32 slot_count = 15;
  // Total probe count across all slots
  uint64 total_probe_count = 16;
  // Total successful probes across all slots
  uint64 total_success_count = 17;
  // Total failed probes across all slots (result = failure)
  uint64 total_failure_count = 18;
  // Total missing probes across all slots (result = missing)
  uint64 total_missing_count = 19;
  // Availability percentage calculated from total counts (total_success_count / total_probe_count * 100, rounded to 2 decimal places)
  double avg_availability_pct = 20;
  // Minimum availability percentage across slots (rounded to 2 decimal places)
  double min_availability_pct = 21;
  // Maximum availability percentage across slots (rounded to 2 decimal places)
  double max_availability_pct = 22;
  // Minimum response time in milliseconds for successful probes only (rounded to whole number)
  uint32 min_response_time_ms = 23;
  // Average of p50 response times across slots for successful probes only (rounded to whole number)
  uint32 avg_p50_response_time_ms = 24;
  // Average of p95 response times across slots for successful probes only (rounded to whole number)
  uint32 avg_p95_response_time_ms = 25;
  // Average of p99 response times across slots for successful probes only (rounded to whole number)
  uint32 avg_p99_response_time_ms = 26;
  // Maximum response time in milliseconds for successful probes only (rounded to whole number)
  uint32 max_response_time_ms = 27;
  // Maximum blob count observed in this epoch
  uint32 max_blob_count = 28;
}

// Request for listing fct_data_column_availability_by_epoch records
message ListFctDataColumnAvailabilityByEpochRequest {
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (PRIMARY KEY - required unless using alternatives: epoch)
  UInt32Filter epoch_start_date_time = 1 [(google.api.field_behavior) = OPTIONAL, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by column_index - Column index (0-127) (ORDER BY column 2 - optional)
  UInt64Filter column_index = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by epoch - Epoch number (ORDER BY column 3 - optional)
  UInt32Filter epoch = 3 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by slot_count - Number of slots in this epoch aggregation (optional)
  UInt32Filter slot_count = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by total_probe_count - Total probe count across all slots (optional)
  UInt64Filter total_probe_count = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by total_success_count - Total successful probes across all slots (optional)
  UInt64Filter total_success_count = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by total_failure_count - Total failed probes across all slots (result = failure) (optional)
  UInt64Filter total_failure_count = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by total_missing_count - Total missing probes across all slots (result = missing) (optional)
  UInt64Filter total_missing_count = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_availability_pct - Availability percentage calculated from total counts (total_success_count / total_probe_count * 100, rounded to 2 decimal places) (optional)
  google.protobuf.DoubleValue avg_availability_pct = 10 [(google.api.field_behavior) = OPTIONAL];
  // Filter by min_availability_pct - Minimum availability percentage across slots (rounded to 2 decimal places) (optional)
  google.protobuf.DoubleValue min_availability_pct = 11 [(google.api.field_behavior) = OPTIONAL];
  // Filter by max_availability_pct - Maximum availability percentage across slots (rounded to 2 decimal places) (optional)
  google.protobuf.DoubleValue max_availability_pct = 12 [(google.api.field_behavior) = OPTIONAL];
  // Filter by min_response_time_ms - Minimum response time in milliseconds for successful probes only (rounded to whole number) (optional)
  UInt32Filter min_response_time_ms = 13 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_p50_response_time_ms - Average of p50 response times across slots for successful probes only (rounded to whole number) (optional)
  UInt32Filter avg_p50_response_time_ms = 14 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_p95_response_time_ms - Average of p95 response times across slots for successful probes only (rounded to whole number) (optional)
  UInt32Filter avg_p95_response_time_ms = 15 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_p99_response_time_ms - Average of p99 response times across slots for successful probes only (rounded to whole number) (optional)
  UInt32Filter avg_p99_response_time_ms = 16 [(google.api.field_behavior) = OPTIONAL];
  // Filter by max_response_time_ms - Maximum response time in milliseconds for successful probes only (rounded to whole number) (optional)
  UInt32Filter max_response_time_ms = 17 [(google.api.field_behavior) = OPTIONAL];
  // Filter by max_blob_count - Maximum blob count observed in this epoch (optional)
  UInt32Filter max_blob_count = 18 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_data_column_availability_by_epoch to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 19 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctDataColumnAvailabilityByEpoch` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 20 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 21 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_data_column_availability_by_epoch records
message ListFctDataColumnAvailabilityByEpochResponse {
  // The list of fct_data_column_availability_by_epoch.
  repeated FctDataColumnAvailabilityByEpoch fct_data_column_availability_by_epoch = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_data_column_availability_by_epoch record by primary key
message GetFctDataColumnAvailabilityByEpochRequest {
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 1; // Primary key (required)
}

// Response for getting a single fct_data_column_availability_by_epoch record
message GetFctDataColumnAvailabilityByEpochResponse {
  FctDataColumnAvailabilityByEpoch item = 1;
}

// Query fct_data_column_availability_by_epoch data
service FctDataColumnAvailabilityByEpochService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctDataColumnAvailabilityByEpochRequest) returns (ListFctDataColumnAvailabilityByEpochResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_data_column_availability_by_epoch"
    };
  }
  // Get record | Retrieve a single record by epoch_start_date_time
  rpc Get(GetFctDataColumnAvailabilityByEpochRequest) returns (GetFctDataColumnAvailabilityByEpochResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_data_column_availability_by_epoch/{epoch_start_date_time}"
    };
  }
}
