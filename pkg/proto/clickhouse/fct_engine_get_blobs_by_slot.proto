syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Slot-level aggregated engine_getBlobs observations grouped by status with duration statistics

message FctEngineGetBlobsBySlot {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Slot number of the beacon block being reconstructed
  uint32 slot = 12;
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 13;
  // Epoch number derived from the slot
  uint32 epoch = 14;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 15;
  // Root of the beacon block (hex encoded with 0x prefix)
  string block_root = 16;
  // Engine API response status (SUCCESS, PARTIAL, EMPTY, UNSUPPORTED, ERROR)
  string status = 17;
  // Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes)
  string node_class = 18;
  // Number of observations for this slot/block/status
  uint32 observation_count = 19;
  // Number of unique nodes that observed this block with this status
  uint32 unique_node_count = 20;
  // Maximum number of versioned hashes requested
  uint32 max_requested_count = 21;
  // Average number of non-null blobs returned
  double avg_returned_count = 22;
  // Percentage of observations where returned_count equals requested_count
  google.protobuf.DoubleValue full_return_pct = 23;
  // Average duration of engine_getBlobs calls in milliseconds
  uint64 avg_duration_ms = 24;
  // Median duration of engine_getBlobs calls in milliseconds
  uint64 median_duration_ms = 25;
  // Minimum duration of engine_getBlobs calls in milliseconds
  uint64 min_duration_ms = 26;
  // Maximum duration of engine_getBlobs calls in milliseconds
  uint64 max_duration_ms = 27;
  // 95th percentile duration of engine_getBlobs calls in milliseconds
  uint64 p95_duration_ms = 28;
  // Number of unique CL client implementations observing this block
  uint32 unique_cl_implementation_count = 29;
  // Number of unique EL client implementations observing this block
  uint32 unique_el_implementation_count = 30;
}

// Request for listing fct_engine_get_blobs_by_slot records
message ListFctEngineGetBlobsBySlotRequest {
  // Filter by slot_start_date_time - The wall clock time when the slot started (PRIMARY KEY - required)
  UInt32Filter slot_start_date_time = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by block_root - Root of the beacon block (hex encoded with 0x prefix) (ORDER BY column 2 - optional)
  StringFilter block_root = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by status - Engine API response status (SUCCESS, PARTIAL, EMPTY, UNSUPPORTED, ERROR) (ORDER BY column 3 - optional)
  StringFilter status = 3 [(google.api.field_behavior) = OPTIONAL];

  // Filter by node_class - Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes) (ORDER BY column 4 - optional)
  StringFilter node_class = 4 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by slot - Slot number of the beacon block being reconstructed (optional)
  UInt32Filter slot = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by epoch - Epoch number derived from the slot (optional)
  UInt32Filter epoch = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by observation_count - Number of observations for this slot/block/status (optional)
  UInt32Filter observation_count = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by unique_node_count - Number of unique nodes that observed this block with this status (optional)
  UInt32Filter unique_node_count = 10 [(google.api.field_behavior) = OPTIONAL];
  // Filter by max_requested_count - Maximum number of versioned hashes requested (optional)
  UInt32Filter max_requested_count = 11 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_returned_count - Average number of non-null blobs returned (optional)
  google.protobuf.DoubleValue avg_returned_count = 12 [(google.api.field_behavior) = OPTIONAL];
  // Filter by full_return_pct - Percentage of observations where returned_count equals requested_count (optional)
  google.protobuf.DoubleValue full_return_pct = 13 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_duration_ms - Average duration of engine_getBlobs calls in milliseconds (optional)
  UInt64Filter avg_duration_ms = 14 [(google.api.field_behavior) = OPTIONAL];
  // Filter by median_duration_ms - Median duration of engine_getBlobs calls in milliseconds (optional)
  UInt64Filter median_duration_ms = 15 [(google.api.field_behavior) = OPTIONAL];
  // Filter by min_duration_ms - Minimum duration of engine_getBlobs calls in milliseconds (optional)
  UInt64Filter min_duration_ms = 16 [(google.api.field_behavior) = OPTIONAL];
  // Filter by max_duration_ms - Maximum duration of engine_getBlobs calls in milliseconds (optional)
  UInt64Filter max_duration_ms = 17 [(google.api.field_behavior) = OPTIONAL];
  // Filter by p95_duration_ms - 95th percentile duration of engine_getBlobs calls in milliseconds (optional)
  UInt64Filter p95_duration_ms = 18 [(google.api.field_behavior) = OPTIONAL];
  // Filter by unique_cl_implementation_count - Number of unique CL client implementations observing this block (optional)
  UInt32Filter unique_cl_implementation_count = 19 [(google.api.field_behavior) = OPTIONAL];
  // Filter by unique_el_implementation_count - Number of unique EL client implementations observing this block (optional)
  UInt32Filter unique_el_implementation_count = 20 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_engine_get_blobs_by_slot to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 21 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctEngineGetBlobsBySlot` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 22 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 23 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_engine_get_blobs_by_slot records
message ListFctEngineGetBlobsBySlotResponse {
  // The list of fct_engine_get_blobs_by_slot.
  repeated FctEngineGetBlobsBySlot fct_engine_get_blobs_by_slot = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_engine_get_blobs_by_slot record by primary key
message GetFctEngineGetBlobsBySlotRequest {
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 1; // Primary key (required)
}

// Response for getting a single fct_engine_get_blobs_by_slot record
message GetFctEngineGetBlobsBySlotResponse {
  FctEngineGetBlobsBySlot item = 1;
}

// Query fct_engine_get_blobs_by_slot data
service FctEngineGetBlobsBySlotService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctEngineGetBlobsBySlotRequest) returns (ListFctEngineGetBlobsBySlotResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_engine_get_blobs_by_slot"
    };
  }
  // Get record | Retrieve a single record by slot_start_date_time
  rpc Get(GetFctEngineGetBlobsBySlotRequest) returns (GetFctEngineGetBlobsBySlotResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_engine_get_blobs_by_slot/{slot_start_date_time}"
    };
  }
}
