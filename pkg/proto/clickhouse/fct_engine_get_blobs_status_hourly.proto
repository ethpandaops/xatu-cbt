syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Hourly aggregated engine_getBlobs status distribution and duration statistics

message FctEngineGetBlobsStatusHourly {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Start of the hour period
  uint32 hour_start_date_time = 12;
  // Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes)
  string node_class = 13;
  // Number of slots in this hour aggregation
  uint32 slot_count = 14;
  // Total number of observations in this hour
  uint64 observation_count = 15;
  // Number of observations with SUCCESS status
  uint64 success_count = 16;
  // Number of observations with PARTIAL status
  uint64 partial_count = 17;
  // Number of observations with EMPTY status
  uint64 empty_count = 18;
  // Number of observations with UNSUPPORTED status
  uint64 unsupported_count = 19;
  // Number of observations with ERROR status
  uint64 error_count = 20;
  // Percentage of observations with SUCCESS status
  double success_pct = 21;
  // Average duration of engine_getBlobs calls in milliseconds
  uint64 avg_duration_ms = 22;
  // Average of median durations across slots in milliseconds
  uint64 avg_p50_duration_ms = 23;
  // Average of p95 durations across slots in milliseconds
  uint64 avg_p95_duration_ms = 24;
  // Maximum duration of engine_getBlobs calls in milliseconds
  uint64 max_duration_ms = 25;
}

// Request for listing fct_engine_get_blobs_status_hourly records
message ListFctEngineGetBlobsStatusHourlyRequest {
  // Filter by hour_start_date_time - Start of the hour period (PRIMARY KEY - required)
  UInt32Filter hour_start_date_time = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by node_class - Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes) (ORDER BY column 2 - optional)
  StringFilter node_class = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 3 [(google.api.field_behavior) = OPTIONAL];
  // Filter by slot_count - Number of slots in this hour aggregation (optional)
  UInt32Filter slot_count = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by observation_count - Total number of observations in this hour (optional)
  UInt64Filter observation_count = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by success_count - Number of observations with SUCCESS status (optional)
  UInt64Filter success_count = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by partial_count - Number of observations with PARTIAL status (optional)
  UInt64Filter partial_count = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by empty_count - Number of observations with EMPTY status (optional)
  UInt64Filter empty_count = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by unsupported_count - Number of observations with UNSUPPORTED status (optional)
  UInt64Filter unsupported_count = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by error_count - Number of observations with ERROR status (optional)
  UInt64Filter error_count = 10 [(google.api.field_behavior) = OPTIONAL];
  // Filter by success_pct - Percentage of observations with SUCCESS status (optional)
  google.protobuf.DoubleValue success_pct = 11 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_duration_ms - Average duration of engine_getBlobs calls in milliseconds (optional)
  UInt64Filter avg_duration_ms = 12 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_p50_duration_ms - Average of median durations across slots in milliseconds (optional)
  UInt64Filter avg_p50_duration_ms = 13 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_p95_duration_ms - Average of p95 durations across slots in milliseconds (optional)
  UInt64Filter avg_p95_duration_ms = 14 [(google.api.field_behavior) = OPTIONAL];
  // Filter by max_duration_ms - Maximum duration of engine_getBlobs calls in milliseconds (optional)
  UInt64Filter max_duration_ms = 15 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_engine_get_blobs_status_hourly to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 16 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctEngineGetBlobsStatusHourly` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 17 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 18 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_engine_get_blobs_status_hourly records
message ListFctEngineGetBlobsStatusHourlyResponse {
  // The list of fct_engine_get_blobs_status_hourly.
  repeated FctEngineGetBlobsStatusHourly fct_engine_get_blobs_status_hourly = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_engine_get_blobs_status_hourly record by primary key
message GetFctEngineGetBlobsStatusHourlyRequest {
  // Start of the hour period
  uint32 hour_start_date_time = 1; // Primary key (required)
}

// Response for getting a single fct_engine_get_blobs_status_hourly record
message GetFctEngineGetBlobsStatusHourlyResponse {
  FctEngineGetBlobsStatusHourly item = 1;
}

// Query fct_engine_get_blobs_status_hourly data
service FctEngineGetBlobsStatusHourlyService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctEngineGetBlobsStatusHourlyRequest) returns (ListFctEngineGetBlobsStatusHourlyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_engine_get_blobs_status_hourly"
    };
  }
  // Get record | Retrieve a single record by hour_start_date_time
  rpc Get(GetFctEngineGetBlobsStatusHourlyRequest) returns (GetFctEngineGetBlobsStatusHourlyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_engine_get_blobs_status_hourly/{hour_start_date_time}"
    };
  }
}
