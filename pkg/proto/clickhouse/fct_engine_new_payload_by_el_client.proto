syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// engine_newPayload observations aggregated by execution client for EL comparison

message FctEngineNewPayloadByElClient {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Slot number of the beacon block containing the payload
  uint32 slot = 12;
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 13;
  // Epoch number derived from the slot
  uint32 epoch = 14;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 15;
  // Root of the beacon block (hex encoded with 0x prefix)
  string block_root = 16;
  // Execution block hash (hex encoded with 0x prefix)
  string block_hash = 17;
  // Consensus client implementation (e.g., lighthouse, prysm, teku)
  string meta_client_implementation = 18;
  // Execution client implementation (e.g., Geth, Nethermind, Besu, Reth)
  string meta_execution_implementation = 19;
  // Whether this observation is from a reference node (controlled fleet with 7870 in name)
  bool is_reference_node = 20;
  // Total gas used by all transactions in the block
  uint64 gas_used = 21;
  // Gas limit of the block
  uint64 gas_limit = 22;
  // Number of transactions in the block
  uint32 tx_count = 23;
  // Number of blobs in the block
  uint32 blob_count = 24;
  // Number of observations for this EL client
  uint32 observation_count = 25;
  // Number of unique nodes with this EL client
  uint32 unique_node_count = 26;
  // Number of observations with VALID status
  uint32 valid_count = 27;
  // Number of observations with INVALID status
  uint32 invalid_count = 28;
  // Number of observations with SYNCING status
  uint32 syncing_count = 29;
  // Number of observations with ACCEPTED status
  uint32 accepted_count = 30;
  // Number of observations with INVALID_BLOCK_HASH status
  uint32 invalid_block_hash_count = 31;
  // Percentage of observations with VALID status
  double valid_pct = 32;
  // Average duration of engine_newPayload calls in milliseconds
  uint64 avg_duration_ms = 33;
  // Median duration of engine_newPayload calls in milliseconds
  uint64 median_duration_ms = 34;
  // Minimum duration of engine_newPayload calls in milliseconds
  uint64 min_duration_ms = 35;
  // Maximum duration of engine_newPayload calls in milliseconds
  uint64 max_duration_ms = 36;
  // 95th percentile duration of engine_newPayload calls in milliseconds
  uint64 p95_duration_ms = 37;
}

// Request for listing fct_engine_new_payload_by_el_client records
message ListFctEngineNewPayloadByElClientRequest {
  // Filter by slot_start_date_time - The wall clock time when the slot started (PRIMARY KEY - required)
  UInt32Filter slot_start_date_time = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by block_hash - Execution block hash (hex encoded with 0x prefix) (ORDER BY column 2 - optional)
  StringFilter block_hash = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by meta_client_implementation - Consensus client implementation (e.g., lighthouse, prysm, teku) (ORDER BY column 3 - optional)
  StringFilter meta_client_implementation = 3 [(google.api.field_behavior) = OPTIONAL];

  // Filter by meta_execution_implementation - Execution client implementation (e.g., Geth, Nethermind, Besu, Reth) (ORDER BY column 4 - optional)
  StringFilter meta_execution_implementation = 4 [(google.api.field_behavior) = OPTIONAL];

  // Filter by is_reference_node - Whether this observation is from a reference node (controlled fleet with 7870 in name) (ORDER BY column 5 - optional)
  BoolFilter is_reference_node = 5 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by slot - Slot number of the beacon block containing the payload (optional)
  UInt32Filter slot = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by epoch - Epoch number derived from the slot (optional)
  UInt32Filter epoch = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by block_root - Root of the beacon block (hex encoded with 0x prefix) (optional)
  StringFilter block_root = 10 [(google.api.field_behavior) = OPTIONAL];
  // Filter by gas_used - Total gas used by all transactions in the block (optional)
  UInt64Filter gas_used = 11 [(google.api.field_behavior) = OPTIONAL];
  // Filter by gas_limit - Gas limit of the block (optional)
  UInt64Filter gas_limit = 12 [(google.api.field_behavior) = OPTIONAL];
  // Filter by tx_count - Number of transactions in the block (optional)
  UInt32Filter tx_count = 13 [(google.api.field_behavior) = OPTIONAL];
  // Filter by blob_count - Number of blobs in the block (optional)
  UInt32Filter blob_count = 14 [(google.api.field_behavior) = OPTIONAL];
  // Filter by observation_count - Number of observations for this EL client (optional)
  UInt32Filter observation_count = 15 [(google.api.field_behavior) = OPTIONAL];
  // Filter by unique_node_count - Number of unique nodes with this EL client (optional)
  UInt32Filter unique_node_count = 16 [(google.api.field_behavior) = OPTIONAL];
  // Filter by valid_count - Number of observations with VALID status (optional)
  UInt32Filter valid_count = 17 [(google.api.field_behavior) = OPTIONAL];
  // Filter by invalid_count - Number of observations with INVALID status (optional)
  UInt32Filter invalid_count = 18 [(google.api.field_behavior) = OPTIONAL];
  // Filter by syncing_count - Number of observations with SYNCING status (optional)
  UInt32Filter syncing_count = 19 [(google.api.field_behavior) = OPTIONAL];
  // Filter by accepted_count - Number of observations with ACCEPTED status (optional)
  UInt32Filter accepted_count = 20 [(google.api.field_behavior) = OPTIONAL];
  // Filter by invalid_block_hash_count - Number of observations with INVALID_BLOCK_HASH status (optional)
  UInt32Filter invalid_block_hash_count = 21 [(google.api.field_behavior) = OPTIONAL];
  // Filter by valid_pct - Percentage of observations with VALID status (optional)
  google.protobuf.DoubleValue valid_pct = 22 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_duration_ms - Average duration of engine_newPayload calls in milliseconds (optional)
  UInt64Filter avg_duration_ms = 23 [(google.api.field_behavior) = OPTIONAL];
  // Filter by median_duration_ms - Median duration of engine_newPayload calls in milliseconds (optional)
  UInt64Filter median_duration_ms = 24 [(google.api.field_behavior) = OPTIONAL];
  // Filter by min_duration_ms - Minimum duration of engine_newPayload calls in milliseconds (optional)
  UInt64Filter min_duration_ms = 25 [(google.api.field_behavior) = OPTIONAL];
  // Filter by max_duration_ms - Maximum duration of engine_newPayload calls in milliseconds (optional)
  UInt64Filter max_duration_ms = 26 [(google.api.field_behavior) = OPTIONAL];
  // Filter by p95_duration_ms - 95th percentile duration of engine_newPayload calls in milliseconds (optional)
  UInt64Filter p95_duration_ms = 27 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_engine_new_payload_by_el_client to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 28 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctEngineNewPayloadByElClient` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 29 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 30 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_engine_new_payload_by_el_client records
message ListFctEngineNewPayloadByElClientResponse {
  // The list of fct_engine_new_payload_by_el_client.
  repeated FctEngineNewPayloadByElClient fct_engine_new_payload_by_el_client = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_engine_new_payload_by_el_client record by primary key
message GetFctEngineNewPayloadByElClientRequest {
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 1; // Primary key (required)
}

// Response for getting a single fct_engine_new_payload_by_el_client record
message GetFctEngineNewPayloadByElClientResponse {
  FctEngineNewPayloadByElClient item = 1;
}

// Query fct_engine_new_payload_by_el_client data
service FctEngineNewPayloadByElClientService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctEngineNewPayloadByElClientRequest) returns (ListFctEngineNewPayloadByElClientResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_engine_new_payload_by_el_client"
    };
  }
  // Get record | Retrieve a single record by slot_start_date_time
  rpc Get(GetFctEngineNewPayloadByElClientRequest) returns (GetFctEngineNewPayloadByElClientResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_engine_new_payload_by_el_client/{slot_start_date_time}"
    };
  }
}
