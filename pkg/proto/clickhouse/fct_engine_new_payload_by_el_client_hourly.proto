syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Hourly aggregated engine_newPayload statistics by execution client with true percentiles

message FctEngineNewPayloadByElClientHourly {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Start of the hour period
  uint32 hour_start_date_time = 12;
  // Execution client implementation (e.g., Geth, Nethermind, Besu, Reth)
  string meta_execution_implementation = 13;
  // Execution client version string
  string meta_execution_version = 14;
  // Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes)
  string node_class = 15;
  // Number of unique slots in this hour
  uint32 slot_count = 16;
  // Total number of observations for this client in this hour
  uint32 observation_count = 17;
  // Number of unique nodes reporting for this client
  uint32 unique_node_count = 18;
  // Number of observations with VALID status
  uint64 valid_count = 19;
  // Number of observations with INVALID status
  uint64 invalid_count = 20;
  // Number of observations with SYNCING status
  uint64 syncing_count = 21;
  // Number of observations with ACCEPTED status
  uint64 accepted_count = 22;
  // Average duration of engine_newPayload calls in milliseconds
  uint64 avg_duration_ms = 23;
  // 50th percentile (median) duration in milliseconds
  uint64 p50_duration_ms = 24;
  // 95th percentile duration in milliseconds
  uint64 p95_duration_ms = 25;
  // Minimum duration in milliseconds
  uint64 min_duration_ms = 26;
  // Maximum duration in milliseconds
  uint64 max_duration_ms = 27;
  // Average gas used per block (VALID status only)
  uint64 avg_gas_used = 28;
  // Average gas limit per block (VALID status only)
  uint64 avg_gas_limit = 29;
  // Average transaction count per block (VALID status only)
  float avg_tx_count = 30;
  // Average blob count per block (VALID status only)
  float avg_blob_count = 31;
}

// Request for listing fct_engine_new_payload_by_el_client_hourly records
message ListFctEngineNewPayloadByElClientHourlyRequest {
  // Filter by hour_start_date_time - Start of the hour period (PRIMARY KEY - required)
  UInt32Filter hour_start_date_time = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by meta_execution_implementation - Execution client implementation (e.g., Geth, Nethermind, Besu, Reth) (ORDER BY column 2 - optional)
  StringFilter meta_execution_implementation = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by meta_execution_version - Execution client version string (ORDER BY column 3 - optional)
  StringFilter meta_execution_version = 3 [(google.api.field_behavior) = OPTIONAL];

  // Filter by node_class - Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes) (ORDER BY column 4 - optional)
  StringFilter node_class = 4 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by slot_count - Number of unique slots in this hour (optional)
  UInt32Filter slot_count = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by observation_count - Total number of observations for this client in this hour (optional)
  UInt32Filter observation_count = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by unique_node_count - Number of unique nodes reporting for this client (optional)
  UInt32Filter unique_node_count = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by valid_count - Number of observations with VALID status (optional)
  UInt64Filter valid_count = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by invalid_count - Number of observations with INVALID status (optional)
  UInt64Filter invalid_count = 10 [(google.api.field_behavior) = OPTIONAL];
  // Filter by syncing_count - Number of observations with SYNCING status (optional)
  UInt64Filter syncing_count = 11 [(google.api.field_behavior) = OPTIONAL];
  // Filter by accepted_count - Number of observations with ACCEPTED status (optional)
  UInt64Filter accepted_count = 12 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_duration_ms - Average duration of engine_newPayload calls in milliseconds (optional)
  UInt64Filter avg_duration_ms = 13 [(google.api.field_behavior) = OPTIONAL];
  // Filter by p50_duration_ms - 50th percentile (median) duration in milliseconds (optional)
  UInt64Filter p50_duration_ms = 14 [(google.api.field_behavior) = OPTIONAL];
  // Filter by p95_duration_ms - 95th percentile duration in milliseconds (optional)
  UInt64Filter p95_duration_ms = 15 [(google.api.field_behavior) = OPTIONAL];
  // Filter by min_duration_ms - Minimum duration in milliseconds (optional)
  UInt64Filter min_duration_ms = 16 [(google.api.field_behavior) = OPTIONAL];
  // Filter by max_duration_ms - Maximum duration in milliseconds (optional)
  UInt64Filter max_duration_ms = 17 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_gas_used - Average gas used per block (VALID status only) (optional)
  UInt64Filter avg_gas_used = 18 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_gas_limit - Average gas limit per block (VALID status only) (optional)
  UInt64Filter avg_gas_limit = 19 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_tx_count - Average transaction count per block (VALID status only) (optional)
  google.protobuf.FloatValue avg_tx_count = 20 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_blob_count - Average blob count per block (VALID status only) (optional)
  google.protobuf.FloatValue avg_blob_count = 21 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_engine_new_payload_by_el_client_hourly to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 22 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctEngineNewPayloadByElClientHourly` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 23 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 24 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_engine_new_payload_by_el_client_hourly records
message ListFctEngineNewPayloadByElClientHourlyResponse {
  // The list of fct_engine_new_payload_by_el_client_hourly.
  repeated FctEngineNewPayloadByElClientHourly fct_engine_new_payload_by_el_client_hourly = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_engine_new_payload_by_el_client_hourly record by primary key
message GetFctEngineNewPayloadByElClientHourlyRequest {
  // Start of the hour period
  uint32 hour_start_date_time = 1; // Primary key (required)
}

// Response for getting a single fct_engine_new_payload_by_el_client_hourly record
message GetFctEngineNewPayloadByElClientHourlyResponse {
  FctEngineNewPayloadByElClientHourly item = 1;
}

// Query fct_engine_new_payload_by_el_client_hourly data
service FctEngineNewPayloadByElClientHourlyService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctEngineNewPayloadByElClientHourlyRequest) returns (ListFctEngineNewPayloadByElClientHourlyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_engine_new_payload_by_el_client_hourly"
    };
  }
  // Get record | Retrieve a single record by hour_start_date_time
  rpc Get(GetFctEngineNewPayloadByElClientHourlyRequest) returns (GetFctEngineNewPayloadByElClientHourlyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_engine_new_payload_by_el_client_hourly/{hour_start_date_time}"
    };
  }
}
