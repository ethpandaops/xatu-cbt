syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Daily aggregated engine_newPayload status distribution and duration statistics

message FctEngineNewPayloadStatusDaily {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Start of the day period
  string day_start_date = 12;
  // Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes)
  string node_class = 13;
  // Number of slots in this day aggregation
  uint32 slot_count = 14;
  // Total number of observations in this day
  uint64 observation_count = 15;
  // Number of observations with VALID status
  uint64 valid_count = 16;
  // Number of observations with INVALID status
  uint64 invalid_count = 17;
  // Number of observations with SYNCING status
  uint64 syncing_count = 18;
  // Number of observations with ACCEPTED status
  uint64 accepted_count = 19;
  // Number of observations with INVALID_BLOCK_HASH status
  uint64 invalid_block_hash_count = 20;
  // Percentage of observations with VALID status
  double valid_pct = 21;
  // Average duration of engine_newPayload calls in milliseconds
  uint64 avg_duration_ms = 22;
  // Average of median durations across slots in milliseconds
  uint64 avg_p50_duration_ms = 23;
  // Average of p95 durations across slots in milliseconds
  uint64 avg_p95_duration_ms = 24;
  // Maximum duration of engine_newPayload calls in milliseconds
  uint64 max_duration_ms = 25;
}

// Request for listing fct_engine_new_payload_status_daily records
message ListFctEngineNewPayloadStatusDailyRequest {
  // Filter by day_start_date - Start of the day period (PRIMARY KEY - required)
  StringFilter day_start_date = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by node_class - Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes) (ORDER BY column 2 - optional)
  StringFilter node_class = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 3 [(google.api.field_behavior) = OPTIONAL];
  // Filter by slot_count - Number of slots in this day aggregation (optional)
  UInt32Filter slot_count = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by observation_count - Total number of observations in this day (optional)
  UInt64Filter observation_count = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by valid_count - Number of observations with VALID status (optional)
  UInt64Filter valid_count = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by invalid_count - Number of observations with INVALID status (optional)
  UInt64Filter invalid_count = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by syncing_count - Number of observations with SYNCING status (optional)
  UInt64Filter syncing_count = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by accepted_count - Number of observations with ACCEPTED status (optional)
  UInt64Filter accepted_count = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by invalid_block_hash_count - Number of observations with INVALID_BLOCK_HASH status (optional)
  UInt64Filter invalid_block_hash_count = 10 [(google.api.field_behavior) = OPTIONAL];
  // Filter by valid_pct - Percentage of observations with VALID status (optional)
  google.protobuf.DoubleValue valid_pct = 11 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_duration_ms - Average duration of engine_newPayload calls in milliseconds (optional)
  UInt64Filter avg_duration_ms = 12 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_p50_duration_ms - Average of median durations across slots in milliseconds (optional)
  UInt64Filter avg_p50_duration_ms = 13 [(google.api.field_behavior) = OPTIONAL];
  // Filter by avg_p95_duration_ms - Average of p95 durations across slots in milliseconds (optional)
  UInt64Filter avg_p95_duration_ms = 14 [(google.api.field_behavior) = OPTIONAL];
  // Filter by max_duration_ms - Maximum duration of engine_newPayload calls in milliseconds (optional)
  UInt64Filter max_duration_ms = 15 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_engine_new_payload_status_daily to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 16 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctEngineNewPayloadStatusDaily` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 17 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 18 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_engine_new_payload_status_daily records
message ListFctEngineNewPayloadStatusDailyResponse {
  // The list of fct_engine_new_payload_status_daily.
  repeated FctEngineNewPayloadStatusDaily fct_engine_new_payload_status_daily = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_engine_new_payload_status_daily record by primary key
message GetFctEngineNewPayloadStatusDailyRequest {
  // Start of the day period
  string day_start_date = 1; // Primary key (required)
}

// Response for getting a single fct_engine_new_payload_status_daily record
message GetFctEngineNewPayloadStatusDailyResponse {
  FctEngineNewPayloadStatusDaily item = 1;
}

// Query fct_engine_new_payload_status_daily data
service FctEngineNewPayloadStatusDailyService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctEngineNewPayloadStatusDailyRequest) returns (ListFctEngineNewPayloadStatusDailyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_engine_new_payload_status_daily"
    };
  }
  // Get record | Retrieve a single record by day_start_date
  rpc Get(GetFctEngineNewPayloadStatusDailyRequest) returns (GetFctEngineNewPayloadStatusDailyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_engine_new_payload_status_daily/{day_start_date}"
    };
  }
}
