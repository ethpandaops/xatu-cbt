syntax = "proto3";

package cbt;

import "common.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Execution layer state size deltas aggregated by month

message FctExecutionStateSizeMonthlyDelta {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // First day of the month
  string month = 12;
  // Account count change within the month
  int64 account_count_delta = 13;
  // Account bytes change within the month
  int64 account_bytes_delta = 14;
  // Account trie nodes change within the month
  int64 account_trienodes_delta = 15;
  // Account trie node bytes change within the month
  int64 account_trienode_bytes_delta = 16;
  // Contract codes change within the month
  int64 contract_codes_delta = 17;
  // Contract code bytes change within the month
  int64 contract_code_bytes_delta = 18;
  // Storage slots change within the month
  int64 storage_count_delta = 19;
  // Storage bytes change within the month
  int64 storage_bytes_delta = 20;
  // Storage trie nodes change within the month
  int64 storage_trienodes_delta = 21;
  // Storage trie node bytes change within the month
  int64 storage_trienode_bytes_delta = 22;
  // Total state size change within the month
  int64 total_bytes_delta = 23;
}

// Request for listing fct_execution_state_size_monthly_delta records
message ListFctExecutionStateSizeMonthlyDeltaRequest {
  // Filter by month - First day of the month (PRIMARY KEY - required)
  StringFilter month = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 2 [(google.api.field_behavior) = OPTIONAL];
  // Filter by account_count_delta - Account count change within the month (optional)
  Int64Filter account_count_delta = 3 [(google.api.field_behavior) = OPTIONAL];
  // Filter by account_bytes_delta - Account bytes change within the month (optional)
  Int64Filter account_bytes_delta = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by account_trienodes_delta - Account trie nodes change within the month (optional)
  Int64Filter account_trienodes_delta = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by account_trienode_bytes_delta - Account trie node bytes change within the month (optional)
  Int64Filter account_trienode_bytes_delta = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by contract_codes_delta - Contract codes change within the month (optional)
  Int64Filter contract_codes_delta = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by contract_code_bytes_delta - Contract code bytes change within the month (optional)
  Int64Filter contract_code_bytes_delta = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by storage_count_delta - Storage slots change within the month (optional)
  Int64Filter storage_count_delta = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by storage_bytes_delta - Storage bytes change within the month (optional)
  Int64Filter storage_bytes_delta = 10 [(google.api.field_behavior) = OPTIONAL];
  // Filter by storage_trienodes_delta - Storage trie nodes change within the month (optional)
  Int64Filter storage_trienodes_delta = 11 [(google.api.field_behavior) = OPTIONAL];
  // Filter by storage_trienode_bytes_delta - Storage trie node bytes change within the month (optional)
  Int64Filter storage_trienode_bytes_delta = 12 [(google.api.field_behavior) = OPTIONAL];
  // Filter by total_bytes_delta - Total state size change within the month (optional)
  Int64Filter total_bytes_delta = 13 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_execution_state_size_monthly_delta to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 14 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctExecutionStateSizeMonthlyDelta` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 15 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 16 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_execution_state_size_monthly_delta records
message ListFctExecutionStateSizeMonthlyDeltaResponse {
  // The list of fct_execution_state_size_monthly_delta.
  repeated FctExecutionStateSizeMonthlyDelta fct_execution_state_size_monthly_delta = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_execution_state_size_monthly_delta record by primary key
message GetFctExecutionStateSizeMonthlyDeltaRequest {
  // First day of the month
  string month = 1; // Primary key (required)
}

// Response for getting a single fct_execution_state_size_monthly_delta record
message GetFctExecutionStateSizeMonthlyDeltaResponse {
  FctExecutionStateSizeMonthlyDelta item = 1;
}

// Query fct_execution_state_size_monthly_delta data
service FctExecutionStateSizeMonthlyDeltaService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctExecutionStateSizeMonthlyDeltaRequest) returns (ListFctExecutionStateSizeMonthlyDeltaResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_execution_state_size_monthly_delta"
    };
  }
  // Get record | Retrieve a single record by month
  rpc Get(GetFctExecutionStateSizeMonthlyDeltaRequest) returns (GetFctExecutionStateSizeMonthlyDeltaResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_execution_state_size_monthly_delta/{month}"
    };
  }
}
