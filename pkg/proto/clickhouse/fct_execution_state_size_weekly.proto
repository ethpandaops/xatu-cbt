syntax = "proto3";

package cbt;

import "common.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Execution layer state size metrics aggregated by week

message FctExecutionStateSizeWeekly {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // First day of the week (Monday)
  string week = 12;
  // Total accounts at end of week
  uint64 accounts = 13;
  // Account bytes at end of week
  uint64 account_bytes = 14;
  // Account trie nodes at end of week
  uint64 account_trienodes = 15;
  // Account trie node bytes at end of week
  uint64 account_trienode_bytes = 16;
  // Contract codes at end of week
  uint64 contract_codes = 17;
  // Contract code bytes at end of week
  uint64 contract_code_bytes = 18;
  // Storage slots at end of week
  uint64 storages = 19;
  // Storage bytes at end of week
  uint64 storage_bytes = 20;
  // Storage trie nodes at end of week
  uint64 storage_trienodes = 21;
  // Storage trie node bytes at end of week
  uint64 storage_trienode_bytes = 22;
  // Total state size in bytes
  uint64 total_bytes = 23;
}

// Request for listing fct_execution_state_size_weekly records
message ListFctExecutionStateSizeWeeklyRequest {
  // Filter by week - First day of the week (Monday) (PRIMARY KEY - required)
  StringFilter week = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 2 [(google.api.field_behavior) = OPTIONAL];
  // Filter by accounts - Total accounts at end of week (optional)
  UInt64Filter accounts = 3 [(google.api.field_behavior) = OPTIONAL];
  // Filter by account_bytes - Account bytes at end of week (optional)
  UInt64Filter account_bytes = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by account_trienodes - Account trie nodes at end of week (optional)
  UInt64Filter account_trienodes = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by account_trienode_bytes - Account trie node bytes at end of week (optional)
  UInt64Filter account_trienode_bytes = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by contract_codes - Contract codes at end of week (optional)
  UInt64Filter contract_codes = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by contract_code_bytes - Contract code bytes at end of week (optional)
  UInt64Filter contract_code_bytes = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by storages - Storage slots at end of week (optional)
  UInt64Filter storages = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by storage_bytes - Storage bytes at end of week (optional)
  UInt64Filter storage_bytes = 10 [(google.api.field_behavior) = OPTIONAL];
  // Filter by storage_trienodes - Storage trie nodes at end of week (optional)
  UInt64Filter storage_trienodes = 11 [(google.api.field_behavior) = OPTIONAL];
  // Filter by storage_trienode_bytes - Storage trie node bytes at end of week (optional)
  UInt64Filter storage_trienode_bytes = 12 [(google.api.field_behavior) = OPTIONAL];
  // Filter by total_bytes - Total state size in bytes (optional)
  UInt64Filter total_bytes = 13 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_execution_state_size_weekly to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 14 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctExecutionStateSizeWeekly` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 15 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 16 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_execution_state_size_weekly records
message ListFctExecutionStateSizeWeeklyResponse {
  // The list of fct_execution_state_size_weekly.
  repeated FctExecutionStateSizeWeekly fct_execution_state_size_weekly = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_execution_state_size_weekly record by primary key
message GetFctExecutionStateSizeWeeklyRequest {
  // First day of the week (Monday)
  string week = 1; // Primary key (required)
}

// Response for getting a single fct_execution_state_size_weekly record
message GetFctExecutionStateSizeWeeklyResponse {
  FctExecutionStateSizeWeekly item = 1;
}

// Query fct_execution_state_size_weekly data
service FctExecutionStateSizeWeeklyService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctExecutionStateSizeWeeklyRequest) returns (ListFctExecutionStateSizeWeeklyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_execution_state_size_weekly"
    };
  }
  // Get record | Retrieve a single record by week
  rpc Get(GetFctExecutionStateSizeWeeklyRequest) returns (GetFctExecutionStateSizeWeeklyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_execution_state_size_weekly/{week}"
    };
  }
}
