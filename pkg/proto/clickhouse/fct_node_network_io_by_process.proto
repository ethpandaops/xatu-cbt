syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Node network I/O per port per sub-slot window with node classification

message FctNodeNetworkIoByProcess {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Start of the sub-slot aggregation window
  int64 window_start = 12;
  // The wallclock slot number
  uint32 wallclock_slot = 13;
  // The wall clock time when the slot started
  int64 wallclock_slot_start_date_time = 14;
  // Name of the observoor client that collected the data
  string meta_client_name = 15;
  // Ethereum network name
  string meta_network_name = 16;
  // Process ID of the monitored client
  uint32 pid = 17;
  // Client type: CL or EL
  string client_type = 18;
  // Port classification (e.g. cl_p2p_tcp, el_json_rpc, unknown)
  string port_label = 19;
  // Traffic direction: tx or rx
  string direction = 20;
  // Total bytes transferred in this window
  float io_bytes = 21;
  // Total packet or event count in this window
  uint32 io_count = 22;
  // Node classification for filtering (e.g. eip7870)
  string node_class = 23;
}

// Request for listing fct_node_network_io_by_process records
message ListFctNodeNetworkIoByProcessRequest {
  // Filter by wallclock_slot_start_date_time - The wall clock time when the slot started (PRIMARY KEY - required)
  Int64Filter wallclock_slot_start_date_time = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by meta_client_name - Name of the observoor client that collected the data (ORDER BY column 2 - optional)
  StringFilter meta_client_name = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by client_type - Client type: CL or EL (ORDER BY column 3 - optional)
  StringFilter client_type = 3 [(google.api.field_behavior) = OPTIONAL];

  // Filter by pid - Process ID of the monitored client (ORDER BY column 4 - optional)
  UInt32Filter pid = 4 [(google.api.field_behavior) = OPTIONAL];

  // Filter by port_label - Port classification (e.g. cl_p2p_tcp, el_json_rpc, unknown) (ORDER BY column 5 - optional)
  StringFilter port_label = 5 [(google.api.field_behavior) = OPTIONAL];

  // Filter by direction - Traffic direction: tx or rx (ORDER BY column 6 - optional)
  StringFilter direction = 6 [(google.api.field_behavior) = OPTIONAL];

  // Filter by window_start - Start of the sub-slot aggregation window (ORDER BY column 7 - optional)
  Int64Filter window_start = 7 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by wallclock_slot - The wallclock slot number (optional)
  UInt32Filter wallclock_slot = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by meta_network_name - Ethereum network name (optional)
  StringFilter meta_network_name = 10 [(google.api.field_behavior) = OPTIONAL];
  // Filter by io_bytes - Total bytes transferred in this window (optional)
  google.protobuf.FloatValue io_bytes = 11 [(google.api.field_behavior) = OPTIONAL];
  // Filter by io_count - Total packet or event count in this window (optional)
  UInt32Filter io_count = 12 [(google.api.field_behavior) = OPTIONAL];
  // Filter by node_class - Node classification for filtering (e.g. eip7870) (optional)
  StringFilter node_class = 13 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_node_network_io_by_process to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 14 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctNodeNetworkIoByProcess` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 15 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 16 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_node_network_io_by_process records
message ListFctNodeNetworkIoByProcessResponse {
  // The list of fct_node_network_io_by_process.
  repeated FctNodeNetworkIoByProcess fct_node_network_io_by_process = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_node_network_io_by_process record by primary key
message GetFctNodeNetworkIoByProcessRequest {
  // The wall clock time when the slot started
  int64 wallclock_slot_start_date_time = 1; // Primary key (required)
}

// Response for getting a single fct_node_network_io_by_process record
message GetFctNodeNetworkIoByProcessResponse {
  FctNodeNetworkIoByProcess item = 1;
}

// Query fct_node_network_io_by_process data
service FctNodeNetworkIoByProcessService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctNodeNetworkIoByProcessRequest) returns (ListFctNodeNetworkIoByProcessResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_node_network_io_by_process"
    };
  }
  // Get record | Retrieve a single record by wallclock_slot_start_date_time
  rpc Get(GetFctNodeNetworkIoByProcessRequest) returns (GetFctNodeNetworkIoByProcessResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_node_network_io_by_process/{wallclock_slot_start_date_time}"
    };
  }
}
