syntax = "proto3";

package cbt;

import "common.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Peer custody bucket distribution aggregated by continent and month

message FctPeerCountByCustodyBucketContinentMonthly {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Start date of the month
  string month_start_date = 12;
  // Continent code of the peer
  string peer_geo_continent_code = 13;
  // Custody group count bucket (1-4, 5-8, 9-16, 17-32, 33-64, 65-127, 128)
  string custody_bucket = 14;
  // Number of epochs in this month
  uint32 epoch_count = 15;
  // Number of distinct peers in this continent and custody bucket in this month
  uint32 peer_count = 16;
}

// Request for listing fct_peer_count_by_custody_bucket_continent_monthly records
message ListFctPeerCountByCustodyBucketContinentMonthlyRequest {
  // Filter by month_start_date - Start date of the month (PRIMARY KEY - required)
  StringFilter month_start_date = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by peer_geo_continent_code - Continent code of the peer (ORDER BY column 2 - optional)
  StringFilter peer_geo_continent_code = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by custody_bucket - Custody group count bucket (1-4, 5-8, 9-16, 17-32, 33-64, 65-127, 128) (ORDER BY column 3 - optional)
  StringFilter custody_bucket = 3 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by epoch_count - Number of epochs in this month (optional)
  UInt32Filter epoch_count = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by peer_count - Number of distinct peers in this continent and custody bucket in this month (optional)
  UInt32Filter peer_count = 6 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_peer_count_by_custody_bucket_continent_monthly to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 7 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctPeerCountByCustodyBucketContinentMonthly` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 8 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 9 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_peer_count_by_custody_bucket_continent_monthly records
message ListFctPeerCountByCustodyBucketContinentMonthlyResponse {
  // The list of fct_peer_count_by_custody_bucket_continent_monthly.
  repeated FctPeerCountByCustodyBucketContinentMonthly fct_peer_count_by_custody_bucket_continent_monthly = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_peer_count_by_custody_bucket_continent_monthly record by primary key
message GetFctPeerCountByCustodyBucketContinentMonthlyRequest {
  // Start date of the month
  string month_start_date = 1; // Primary key (required)
}

// Response for getting a single fct_peer_count_by_custody_bucket_continent_monthly record
message GetFctPeerCountByCustodyBucketContinentMonthlyResponse {
  FctPeerCountByCustodyBucketContinentMonthly item = 1;
}

// Query fct_peer_count_by_custody_bucket_continent_monthly data
service FctPeerCountByCustodyBucketContinentMonthlyService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctPeerCountByCustodyBucketContinentMonthlyRequest) returns (ListFctPeerCountByCustodyBucketContinentMonthlyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_peer_count_by_custody_bucket_continent_monthly"
    };
  }
  // Get record | Retrieve a single record by month_start_date
  rpc Get(GetFctPeerCountByCustodyBucketContinentMonthlyRequest) returns (GetFctPeerCountByCustodyBucketContinentMonthlyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_peer_count_by_custody_bucket_continent_monthly/{month_start_date}"
    };
  }
}
