syntax = "proto3";

package cbt;

import "common.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Storage slot state metrics aggregated by hour

message FctStorageSlotStateByBlockHourly {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Start of the hour period
  uint32 hour_start_date_time = 12;
  // Cumulative count of active storage slots at end of hour
  int64 active_slots = 13;
  // Cumulative sum of effective bytes at end of hour
  int64 effective_bytes = 14;
}

// Request for listing fct_storage_slot_state_by_block_hourly records
message ListFctStorageSlotStateByBlockHourlyRequest {
  // Filter by hour_start_date_time - Start of the hour period (PRIMARY KEY - required)
  UInt32Filter hour_start_date_time = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 2 [(google.api.field_behavior) = OPTIONAL];
  // Filter by active_slots - Cumulative count of active storage slots at end of hour (optional)
  Int64Filter active_slots = 3 [(google.api.field_behavior) = OPTIONAL];
  // Filter by effective_bytes - Cumulative sum of effective bytes at end of hour (optional)
  Int64Filter effective_bytes = 4 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_storage_slot_state_by_block_hourly to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 5 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctStorageSlotStateByBlockHourly` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 6 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 7 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_storage_slot_state_by_block_hourly records
message ListFctStorageSlotStateByBlockHourlyResponse {
  // The list of fct_storage_slot_state_by_block_hourly.
  repeated FctStorageSlotStateByBlockHourly fct_storage_slot_state_by_block_hourly = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_storage_slot_state_by_block_hourly record by primary key
message GetFctStorageSlotStateByBlockHourlyRequest {
  // Start of the hour period
  uint32 hour_start_date_time = 1; // Primary key (required)
}

// Response for getting a single fct_storage_slot_state_by_block_hourly record
message GetFctStorageSlotStateByBlockHourlyResponse {
  FctStorageSlotStateByBlockHourly item = 1;
}

// Query fct_storage_slot_state_by_block_hourly data
service FctStorageSlotStateByBlockHourlyService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctStorageSlotStateByBlockHourlyRequest) returns (ListFctStorageSlotStateByBlockHourlyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_storage_slot_state_by_block_hourly"
    };
  }
  // Get record | Retrieve a single record by hour_start_date_time
  rpc Get(GetFctStorageSlotStateByBlockHourlyRequest) returns (GetFctStorageSlotStateByBlockHourlyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_storage_slot_state_by_block_hourly/{hour_start_date_time}"
    };
  }
}
