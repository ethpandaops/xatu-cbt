syntax = "proto3";

package cbt;

import "common.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Cumulative storage slot state per block with 6-month expiry policy applied - slots unused for 6 months are cleared

message FctStorageSlotStateWithExpiryBy6m {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // The block number
  uint32 block_number = 12;
  // Net slot adjustment this block (negative=expiry, positive=reactivation)
  int32 net_slots_delta = 13;
  // Net bytes adjustment this block (negative=expiry, positive=reactivation)
  int64 net_bytes_delta = 14;
  // Cumulative net slot adjustment up to this block
  int64 cumulative_net_slots = 15;
  // Cumulative net bytes adjustment up to this block
  int64 cumulative_net_bytes = 16;
  // Cumulative count of active storage slots at this block (with 6-month expiry applied)
  int64 active_slots = 17;
  // Cumulative sum of effective bytes across all active slots at this block (with 6-month expiry applied)
  int64 effective_bytes = 18;
}

// Request for listing fct_storage_slot_state_with_expiry_by_6m records
message ListFctStorageSlotStateWithExpiryBy6mRequest {
  // Filter by block_number - The block number (PRIMARY KEY - required)
  UInt32Filter block_number = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 2 [(google.api.field_behavior) = OPTIONAL];
  // Filter by net_slots_delta - Net slot adjustment this block (negative=expiry, positive=reactivation) (optional)
  Int32Filter net_slots_delta = 3 [(google.api.field_behavior) = OPTIONAL];
  // Filter by net_bytes_delta - Net bytes adjustment this block (negative=expiry, positive=reactivation) (optional)
  Int64Filter net_bytes_delta = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by cumulative_net_slots - Cumulative net slot adjustment up to this block (optional)
  Int64Filter cumulative_net_slots = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by cumulative_net_bytes - Cumulative net bytes adjustment up to this block (optional)
  Int64Filter cumulative_net_bytes = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by active_slots - Cumulative count of active storage slots at this block (with 6-month expiry applied) (optional)
  Int64Filter active_slots = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by effective_bytes - Cumulative sum of effective bytes across all active slots at this block (with 6-month expiry applied) (optional)
  Int64Filter effective_bytes = 8 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_storage_slot_state_with_expiry_by_6m to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 9 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctStorageSlotStateWithExpiryBy6m` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 10 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 11 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_storage_slot_state_with_expiry_by_6m records
message ListFctStorageSlotStateWithExpiryBy6mResponse {
  // The list of fct_storage_slot_state_with_expiry_by_6m.
  repeated FctStorageSlotStateWithExpiryBy6m fct_storage_slot_state_with_expiry_by_6m = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_storage_slot_state_with_expiry_by_6m record by primary key
message GetFctStorageSlotStateWithExpiryBy6mRequest {
  // The block number
  uint32 block_number = 1; // Primary key (required)
}

// Response for getting a single fct_storage_slot_state_with_expiry_by_6m record
message GetFctStorageSlotStateWithExpiryBy6mResponse {
  FctStorageSlotStateWithExpiryBy6m item = 1;
}

// Query fct_storage_slot_state_with_expiry_by_6m data
service FctStorageSlotStateWithExpiryBy6mService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctStorageSlotStateWithExpiryBy6mRequest) returns (ListFctStorageSlotStateWithExpiryBy6mResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_storage_slot_state_with_expiry_by_6m"
    };
  }
  // Get record | Retrieve a single record by block_number
  rpc Get(GetFctStorageSlotStateWithExpiryBy6mRequest) returns (GetFctStorageSlotStateWithExpiryBy6mResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_storage_slot_state_with_expiry_by_6m/{block_number}"
    };
  }
}
