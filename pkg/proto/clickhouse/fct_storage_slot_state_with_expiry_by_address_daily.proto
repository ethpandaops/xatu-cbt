syntax = "proto3";

package cbt;

import "common.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Storage slot state metrics per address with expiry policies aggregated by day

message FctStorageSlotStateWithExpiryByAddressDaily {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // The contract address
  string address = 12;
  // Start of the day period
  string day_start_date = 13;
  // Expiry policy identifier: 1m, 6m, 12m, 18m, 24m
  string expiry_policy = 14;
  // Cumulative count of active storage slots at end of day (with expiry applied)
  int64 active_slots = 15;
  // Cumulative sum of effective bytes at end of day (with expiry applied)
  int64 effective_bytes = 16;
}

// Request for listing fct_storage_slot_state_with_expiry_by_address_daily records
message ListFctStorageSlotStateWithExpiryByAddressDailyRequest {
  // Filter by address - The contract address (PRIMARY KEY - required)
  StringFilter address = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by expiry_policy - Expiry policy identifier: 1m, 6m, 12m, 18m, 24m (ORDER BY column 2 - optional)
  StringFilter expiry_policy = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by day_start_date - Start of the day period (ORDER BY column 3 - optional)
  StringFilter day_start_date = 3 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by active_slots - Cumulative count of active storage slots at end of day (with expiry applied) (optional)
  Int64Filter active_slots = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by effective_bytes - Cumulative sum of effective bytes at end of day (with expiry applied) (optional)
  Int64Filter effective_bytes = 6 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_storage_slot_state_with_expiry_by_address_daily to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 7 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctStorageSlotStateWithExpiryByAddressDaily` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 8 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 9 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_storage_slot_state_with_expiry_by_address_daily records
message ListFctStorageSlotStateWithExpiryByAddressDailyResponse {
  // The list of fct_storage_slot_state_with_expiry_by_address_daily.
  repeated FctStorageSlotStateWithExpiryByAddressDaily fct_storage_slot_state_with_expiry_by_address_daily = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_storage_slot_state_with_expiry_by_address_daily record by primary key
message GetFctStorageSlotStateWithExpiryByAddressDailyRequest {
  // The contract address
  string address = 1; // Primary key (required)
}

// Response for getting a single fct_storage_slot_state_with_expiry_by_address_daily record
message GetFctStorageSlotStateWithExpiryByAddressDailyResponse {
  FctStorageSlotStateWithExpiryByAddressDaily item = 1;
}

// Query fct_storage_slot_state_with_expiry_by_address_daily data
service FctStorageSlotStateWithExpiryByAddressDailyService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctStorageSlotStateWithExpiryByAddressDailyRequest) returns (ListFctStorageSlotStateWithExpiryByAddressDailyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_storage_slot_state_with_expiry_by_address_daily"
    };
  }
  // Get record | Retrieve a single record by address
  rpc Get(GetFctStorageSlotStateWithExpiryByAddressDailyRequest) returns (GetFctStorageSlotStateWithExpiryByAddressDailyResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_storage_slot_state_with_expiry_by_address_daily/{address}"
    };
  }
}
