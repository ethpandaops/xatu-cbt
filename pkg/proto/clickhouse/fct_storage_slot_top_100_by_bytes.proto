syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Top 100 contracts by effective storage bytes with expiry policies applied

message FctStorageSlotTop100ByBytes {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Expiry policy identifier: NULL (raw), 1m, 6m, 12m, 18m, 24m
  google.protobuf.StringValue expiry_policy = 12;
  // Rank by effective bytes (1=highest), based on raw state
  uint32 rank = 13;
  // The contract address
  string contract_address = 14;
  // Effective bytes of storage for this contract
  int64 effective_bytes = 15;
  // Number of active storage slots for this contract
  int64 active_slots = 16;
  // Owner key identifier
  google.protobuf.StringValue owner_key = 17;
  // Account owner of the contract
  google.protobuf.StringValue account_owner = 18;
  // Name of the contract
  google.protobuf.StringValue contract_name = 19;
  // Factory contract or deployer address
  google.protobuf.StringValue factory_contract = 20;
  // Labels/categories (e.g., stablecoin, dex, circle)
  repeated string labels = 21;
}

// Request for listing fct_storage_slot_top_100_by_bytes records
message ListFctStorageSlotTop100ByBytesRequest {
  // Filter by rank - Rank by effective bytes (1=highest), based on raw state (PRIMARY KEY - required)
  UInt32Filter rank = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 2 [(google.api.field_behavior) = OPTIONAL];
  // Filter by expiry_policy - Expiry policy identifier: NULL (raw), 1m, 6m, 12m, 18m, 24m (optional)
  NullableStringFilter expiry_policy = 3 [(google.api.field_behavior) = OPTIONAL];
  // Filter by contract_address - The contract address (optional)
  StringFilter contract_address = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by effective_bytes - Effective bytes of storage for this contract (optional)
  Int64Filter effective_bytes = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by active_slots - Number of active storage slots for this contract (optional)
  Int64Filter active_slots = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by owner_key - Owner key identifier (optional)
  NullableStringFilter owner_key = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by account_owner - Account owner of the contract (optional)
  NullableStringFilter account_owner = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by contract_name - Name of the contract (optional)
  NullableStringFilter contract_name = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by factory_contract - Factory contract or deployer address (optional)
  NullableStringFilter factory_contract = 10 [(google.api.field_behavior) = OPTIONAL];
  // Filter by labels - Labels/categories (e.g., stablecoin, dex, circle) (optional)
  ArrayStringFilter labels = 11 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_storage_slot_top_100_by_bytes to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 12 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctStorageSlotTop100ByBytes` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 13 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 14 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_storage_slot_top_100_by_bytes records
message ListFctStorageSlotTop100ByBytesResponse {
  // The list of fct_storage_slot_top_100_by_bytes.
  repeated FctStorageSlotTop100ByBytes fct_storage_slot_top_100_by_bytes = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_storage_slot_top_100_by_bytes record by primary key
message GetFctStorageSlotTop100ByBytesRequest {
  // Rank by effective bytes (1=highest), based on raw state
  uint32 rank = 1; // Primary key (required)
}

// Response for getting a single fct_storage_slot_top_100_by_bytes record
message GetFctStorageSlotTop100ByBytesResponse {
  FctStorageSlotTop100ByBytes item = 1;
}

// Query fct_storage_slot_top_100_by_bytes data
service FctStorageSlotTop100ByBytesService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctStorageSlotTop100ByBytesRequest) returns (ListFctStorageSlotTop100ByBytesResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_storage_slot_top_100_by_bytes"
    };
  }
  // Get record | Retrieve a single record by rank
  rpc Get(GetFctStorageSlotTop100ByBytesRequest) returns (GetFctStorageSlotTop100ByBytesResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_storage_slot_top_100_by_bytes/{rank}"
    };
  }
}
