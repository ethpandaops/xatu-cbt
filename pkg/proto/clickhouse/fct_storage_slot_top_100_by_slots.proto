syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Top 100 contracts by active storage slot count

message FctStorageSlotTop100BySlots {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Rank by active slots (1=highest)
  uint32 rank = 12;
  // The contract address
  string contract_address = 13;
  // Number of active storage slots for this contract
  int64 active_slots = 14;
  // Effective bytes of storage for this contract
  int64 effective_bytes = 15;
  // Owner key identifier
  google.protobuf.StringValue owner_key = 16;
  // Account owner of the contract
  google.protobuf.StringValue account_owner = 17;
  // Name of the contract
  google.protobuf.StringValue contract_name = 18;
  // Factory contract or deployer address
  google.protobuf.StringValue factory_contract = 19;
  // Usage category (e.g., stablecoin, dex, trading)
  google.protobuf.StringValue usage_category = 20;
}

// Request for listing fct_storage_slot_top_100_by_slots records
message ListFctStorageSlotTop100BySlotsRequest {
  // Filter by rank - Rank by active slots (1=highest) (PRIMARY KEY - required)
  UInt32Filter rank = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 2 [(google.api.field_behavior) = OPTIONAL];
  // Filter by contract_address - The contract address (optional)
  StringFilter contract_address = 3 [(google.api.field_behavior) = OPTIONAL];
  // Filter by active_slots - Number of active storage slots for this contract (optional)
  Int64Filter active_slots = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by effective_bytes - Effective bytes of storage for this contract (optional)
  Int64Filter effective_bytes = 5 [(google.api.field_behavior) = OPTIONAL];
  // Filter by owner_key - Owner key identifier (optional)
  NullableStringFilter owner_key = 6 [(google.api.field_behavior) = OPTIONAL];
  // Filter by account_owner - Account owner of the contract (optional)
  NullableStringFilter account_owner = 7 [(google.api.field_behavior) = OPTIONAL];
  // Filter by contract_name - Name of the contract (optional)
  NullableStringFilter contract_name = 8 [(google.api.field_behavior) = OPTIONAL];
  // Filter by factory_contract - Factory contract or deployer address (optional)
  NullableStringFilter factory_contract = 9 [(google.api.field_behavior) = OPTIONAL];
  // Filter by usage_category - Usage category (e.g., stablecoin, dex, trading) (optional)
  NullableStringFilter usage_category = 10 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_storage_slot_top_100_by_slots to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 11 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctStorageSlotTop100BySlots` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 12 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 13 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_storage_slot_top_100_by_slots records
message ListFctStorageSlotTop100BySlotsResponse {
  // The list of fct_storage_slot_top_100_by_slots.
  repeated FctStorageSlotTop100BySlots fct_storage_slot_top_100_by_slots = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_storage_slot_top_100_by_slots record by primary key
message GetFctStorageSlotTop100BySlotsRequest {
  // Rank by active slots (1=highest)
  uint32 rank = 1; // Primary key (required)
}

// Response for getting a single fct_storage_slot_top_100_by_slots record
message GetFctStorageSlotTop100BySlotsResponse {
  FctStorageSlotTop100BySlots item = 1;
}

// Query fct_storage_slot_top_100_by_slots data
service FctStorageSlotTop100BySlotsService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctStorageSlotTop100BySlotsRequest) returns (ListFctStorageSlotTop100BySlotsResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_storage_slot_top_100_by_slots"
    };
  }
  // Get record | Retrieve a single record by rank
  rpc Get(GetFctStorageSlotTop100BySlotsRequest) returns (GetFctStorageSlotTop100BySlotsResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_storage_slot_top_100_by_slots/{rank}"
    };
  }
}
