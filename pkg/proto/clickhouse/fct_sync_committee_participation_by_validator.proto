syntax = "proto3";

package cbt;

import "common.proto";
import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "clickhouse/annotations.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Per-slot sync committee participation by validator

message FctSyncCommitteeParticipationByValidator {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // The slot number
  uint64 slot = 12;
  // The start time of the slot
  uint32 slot_start_date_time = 13;
  // Index of the validator
  uint32 validator_index = 14;
  // Whether the validator participated in sync committee for this slot
  bool participated = 15;
}

// Request for listing fct_sync_committee_participation_by_validator records
message ListFctSyncCommitteeParticipationByValidatorRequest {
  // Filter by validator_index - Index of the validator (PRIMARY KEY - required)
  UInt32Filter validator_index = 1 [(google.api.field_behavior) = REQUIRED, (clickhouse.v1.required_group) = "primary_key"];

  // Filter by slot_start_date_time - The start time of the slot (ORDER BY column 2 - optional)
  UInt32Filter slot_start_date_time = 2 [(google.api.field_behavior) = OPTIONAL];

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 3 [(google.api.field_behavior) = OPTIONAL];
  // Filter by slot - The slot number (optional)
  UInt64Filter slot = 4 [(google.api.field_behavior) = OPTIONAL];
  // Filter by participated - Whether the validator participated in sync committee for this slot (optional)
  BoolFilter participated = 5 [(google.api.field_behavior) = OPTIONAL];

  // The maximum number of fct_sync_committee_participation_by_validator to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 6 [(google.api.field_behavior) = OPTIONAL];
  // A page token, received from a previous `ListFctSyncCommitteeParticipationByValidator` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 7 [(google.api.field_behavior) = OPTIONAL];
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 8 [(google.api.field_behavior) = OPTIONAL];
}

// Response for listing fct_sync_committee_participation_by_validator records
message ListFctSyncCommitteeParticipationByValidatorResponse {
  // The list of fct_sync_committee_participation_by_validator.
  repeated FctSyncCommitteeParticipationByValidator fct_sync_committee_participation_by_validator = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single fct_sync_committee_participation_by_validator record by primary key
message GetFctSyncCommitteeParticipationByValidatorRequest {
  // Index of the validator
  uint32 validator_index = 1; // Primary key (required)
}

// Response for getting a single fct_sync_committee_participation_by_validator record
message GetFctSyncCommitteeParticipationByValidatorResponse {
  FctSyncCommitteeParticipationByValidator item = 1;
}

// Query fct_sync_committee_participation_by_validator data
service FctSyncCommitteeParticipationByValidatorService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListFctSyncCommitteeParticipationByValidatorRequest) returns (ListFctSyncCommitteeParticipationByValidatorResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_sync_committee_participation_by_validator"
    };
  }
  // Get record | Retrieve a single record by validator_index
  rpc Get(GetFctSyncCommitteeParticipationByValidatorRequest) returns (GetFctSyncCommitteeParticipationByValidatorResponse) {
    option (google.api.http) = {
      get: "/api/v1/fct_sync_committee_participation_by_validator/{validator_index}"
    };
  }
}
