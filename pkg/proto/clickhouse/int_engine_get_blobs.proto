syntax = "proto3";

package cbt;

import "common.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Individual engine_getBlobs observations enriched with slot context from beacon blob sidecar data

message IntEngineGetBlobs {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // When the sentry received the event
  int64 event_date_time = 12;
  // When the engine_getBlobs call was initiated
  int64 requested_date_time = 13;
  // How long the engine_getBlobs call took in milliseconds
  uint32 duration_ms = 14;
  // Slot number of the beacon block containing the blobs
  uint32 slot = 15;
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 16;
  // Epoch number derived from the slot
  uint32 epoch = 17;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 18;
  // Root of the beacon block (hex encoded with 0x prefix)
  string block_root = 19;
  // Root of the parent beacon block (hex encoded with 0x prefix)
  string block_parent_root = 20;
  // Validator index of the block proposer
  uint32 proposer_index = 21;
  // Number of blobs requested (length of versioned_hashes array)
  uint32 requested_count = 22;
  // Versioned hashes of the requested blobs
  repeated string versioned_hashes = 23;
  // Number of blobs actually returned
  uint32 returned_count = 24;
  // Indexes of the returned blobs
  repeated uint32 returned_blob_indexes = 25;
  // Engine API response status (SUCCESS, PARTIAL, EMPTY, UNSUPPORTED, ERROR)
  string status = 26;
  // Error message when the call fails
  google.protobuf.StringValue error_message = 27;
  // Version of the engine_getBlobs method
  string method_version = 28;
  // Source of the engine event
  string source = 29;
  // Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes)
  string node_class = 30;
  // Full execution client version string from web3_clientVersion RPC
  string meta_execution_version = 31;
  // Execution client implementation name (e.g., Geth, Nethermind, Besu, Reth)
  string meta_execution_implementation = 32;
  // Name of the client that generated the event
  string meta_client_name = 33;
  // Implementation of the client that generated the event
  string meta_client_implementation = 34;
  // Version of the client that generated the event
  string meta_client_version = 35;
  // City of the client that generated the event
  string meta_client_geo_city = 36;
  // Country of the client that generated the event
  string meta_client_geo_country = 37;
  // Country code of the client that generated the event
  string meta_client_geo_country_code = 38;
  // Continent code of the client that generated the event
  string meta_client_geo_continent_code = 39;
  // Latitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_latitude = 40;
  // Longitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_longitude = 41;
  // Autonomous system number of the client that generated the event
  google.protobuf.UInt32Value meta_client_geo_autonomous_system_number = 42;
  // Autonomous system organization of the client that generated the event
  google.protobuf.StringValue meta_client_geo_autonomous_system_organization = 43;
}

// Request for listing int_engine_get_blobs records
message ListIntEngineGetBlobsRequest {
  // Filter by slot_start_date_time - The wall clock time when the slot started (PRIMARY KEY - required unless using alternatives: duration_ms, slot)
  UInt32Filter slot_start_date_time = 1;

  // Filter by block_root - Root of the beacon block (hex encoded with 0x prefix) (ORDER BY column 2 - optional)
  StringFilter block_root = 2;

  // Filter by meta_client_name - Name of the client that generated the event (ORDER BY column 3 - optional)
  StringFilter meta_client_name = 3;

  // Filter by event_date_time - When the sentry received the event (ORDER BY column 4 - optional)
  Int64Filter event_date_time = 4;

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 5;
  // Filter by requested_date_time - When the engine_getBlobs call was initiated (optional)
  Int64Filter requested_date_time = 6;
  // Filter by duration_ms - How long the engine_getBlobs call took in milliseconds (PROJECTION: p_by_duration_ms - alternative to slot_start_date_time)
  UInt32Filter duration_ms = 7;
  // Filter by slot - Slot number of the beacon block containing the blobs (PROJECTION: p_by_slot - alternative to slot_start_date_time)
  UInt32Filter slot = 8;
  // Filter by epoch - Epoch number derived from the slot (optional)
  UInt32Filter epoch = 9;
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 10;
  // Filter by block_parent_root - Root of the parent beacon block (hex encoded with 0x prefix) (optional)
  StringFilter block_parent_root = 11;
  // Filter by proposer_index - Validator index of the block proposer (optional)
  UInt32Filter proposer_index = 12;
  // Filter by requested_count - Number of blobs requested (length of versioned_hashes array) (optional)
  UInt32Filter requested_count = 13;
  // Filter by versioned_hashes - Versioned hashes of the requested blobs (optional)
  ArrayStringFilter versioned_hashes = 14;
  // Filter by returned_count - Number of blobs actually returned (optional)
  UInt32Filter returned_count = 15;
  // Filter by returned_blob_indexes - Indexes of the returned blobs (optional)
  ArrayUInt32Filter returned_blob_indexes = 16;
  // Filter by status - Engine API response status (SUCCESS, PARTIAL, EMPTY, UNSUPPORTED, ERROR) (optional)
  StringFilter status = 17;
  // Filter by error_message - Error message when the call fails (optional)
  NullableStringFilter error_message = 18;
  // Filter by method_version - Version of the engine_getBlobs method (optional)
  StringFilter method_version = 19;
  // Filter by source - Source of the engine event (optional)
  StringFilter source = 20;
  // Filter by node_class - Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes) (optional)
  StringFilter node_class = 21;
  // Filter by meta_execution_version - Full execution client version string from web3_clientVersion RPC (optional)
  StringFilter meta_execution_version = 22;
  // Filter by meta_execution_implementation - Execution client implementation name (e.g., Geth, Nethermind, Besu, Reth) (optional)
  StringFilter meta_execution_implementation = 23;
  // Filter by meta_client_implementation - Implementation of the client that generated the event (optional)
  StringFilter meta_client_implementation = 24;
  // Filter by meta_client_version - Version of the client that generated the event (optional)
  StringFilter meta_client_version = 25;
  // Filter by meta_client_geo_city - City of the client that generated the event (optional)
  StringFilter meta_client_geo_city = 26;
  // Filter by meta_client_geo_country - Country of the client that generated the event (optional)
  StringFilter meta_client_geo_country = 27;
  // Filter by meta_client_geo_country_code - Country code of the client that generated the event (optional)
  StringFilter meta_client_geo_country_code = 28;
  // Filter by meta_client_geo_continent_code - Continent code of the client that generated the event (optional)
  StringFilter meta_client_geo_continent_code = 29;
  // Filter by meta_client_geo_latitude - Latitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_latitude = 30;
  // Filter by meta_client_geo_longitude - Longitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_longitude = 31;
  // Filter by meta_client_geo_autonomous_system_number - Autonomous system number of the client that generated the event (optional)
  NullableUInt32Filter meta_client_geo_autonomous_system_number = 32;
  // Filter by meta_client_geo_autonomous_system_organization - Autonomous system organization of the client that generated the event (optional)
  NullableStringFilter meta_client_geo_autonomous_system_organization = 33;

  // The maximum number of int_engine_get_blobs to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 34;
  // A page token, received from a previous `ListIntEngineGetBlobs` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 35;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 36;
}

// Response for listing int_engine_get_blobs records
message ListIntEngineGetBlobsResponse {
  // The list of int_engine_get_blobs.
  repeated IntEngineGetBlobs int_engine_get_blobs = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single int_engine_get_blobs record by primary key
message GetIntEngineGetBlobsRequest {
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 1; // Primary key (required)
}

// Response for getting a single int_engine_get_blobs record
message GetIntEngineGetBlobsResponse {
  IntEngineGetBlobs item = 1;
}

// Query int_engine_get_blobs data
service IntEngineGetBlobsService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListIntEngineGetBlobsRequest) returns (ListIntEngineGetBlobsResponse);
  // Get record | Retrieve a single record by primary key
  rpc Get(GetIntEngineGetBlobsRequest) returns (GetIntEngineGetBlobsResponse);
}
