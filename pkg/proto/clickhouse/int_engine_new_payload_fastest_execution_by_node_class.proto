syntax = "proto3";

package cbt;

import "common.proto";

option go_package = "github.com/ethpandaops/xatu-cbt/pkg/proto/clickhouse";
// Fastest valid engine_newPayload observation per slot per node_class

message IntEngineNewPayloadFastestExecutionByNodeClass {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Slot number of the beacon block containing the payload
  uint32 slot = 12;
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 13;
  // Epoch number derived from the slot
  uint32 epoch = 14;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 15;
  // Execution block hash (hex encoded with 0x prefix)
  string block_hash = 16;
  // Duration of the fastest engine_newPayload call in milliseconds
  uint64 duration_ms = 17;
  // Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes)
  string node_class = 18;
  // Execution client implementation name (e.g., Geth, Nethermind, Besu, Reth)
  string meta_execution_implementation = 19;
  // Full execution client version string from web3_clientVersion RPC
  string meta_execution_version = 20;
  // Name of the client that generated the event
  string meta_client_name = 21;
}

// Request for listing int_engine_new_payload_fastest records
message ListIntEngineNewPayloadFastestExecutionByNodeClassRequest {
  // Filter by slot_start_date_time - The wall clock time when the slot started (PRIMARY KEY - required)
  UInt32Filter slot_start_date_time = 1;

  // Filter by slot - Slot number of the beacon block containing the payload (ORDER BY column 2 - optional)
  UInt32Filter slot = 2;

  // Filter by node_class - Node classification for grouping observations (e.g., eip7870-block-builder, or empty for general nodes) (ORDER BY column 3 - optional)
  StringFilter node_class = 3;

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 4;
  // Filter by epoch - Epoch number derived from the slot (optional)
  UInt32Filter epoch = 5;
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 6;
  // Filter by block_hash - Execution block hash (hex encoded with 0x prefix) (optional)
  StringFilter block_hash = 7;
  // Filter by duration_ms - Duration of the fastest engine_newPayload call in milliseconds (optional)
  UInt64Filter duration_ms = 8;
  // Filter by meta_execution_implementation - Execution client implementation name (e.g., Geth, Nethermind, Besu, Reth) (optional)
  StringFilter meta_execution_implementation = 9;
  // Filter by meta_execution_version - Full execution client version string from web3_clientVersion RPC (optional)
  StringFilter meta_execution_version = 10;
  // Filter by meta_client_name - Name of the client that generated the event (optional)
  StringFilter meta_client_name = 11;

  // The maximum number of int_engine_new_payload_fastest to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 12;
  // A page token, received from a previous `ListIntEngineNewPayloadFastestExecutionByNodeClass` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 13;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 14;
}

// Response for listing int_engine_new_payload_fastest records
message ListIntEngineNewPayloadFastestExecutionByNodeClassResponse {
  // The list of int_engine_new_payload_fastest.
  repeated IntEngineNewPayloadFastestExecutionByNodeClass int_engine_new_payload_fastest = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single int_engine_new_payload_fastest record by primary key
message GetIntEngineNewPayloadFastestExecutionByNodeClassRequest {
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 1; // Primary key (required)
}

// Response for getting a single int_engine_new_payload_fastest record
message GetIntEngineNewPayloadFastestExecutionByNodeClassResponse {
  IntEngineNewPayloadFastestExecutionByNodeClass item = 1;
}

// Query int_engine_new_payload_fastest data
service IntEngineNewPayloadFastestExecutionByNodeClassService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListIntEngineNewPayloadFastestExecutionByNodeClassRequest) returns (ListIntEngineNewPayloadFastestExecutionByNodeClassResponse);
  // Get record | Retrieve a single record by primary key
  rpc Get(GetIntEngineNewPayloadFastestExecutionByNodeClassRequest) returns (GetIntEngineNewPayloadFastestExecutionByNodeClassResponse);
}
