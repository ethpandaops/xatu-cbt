model: canonical_beacon_proposer_duty
network: mainnet
external_data:
    canonical_beacon_proposer_duty:
        url: https://data.ethpandaops.io/xatu-cbt/tests/mainnet/canonical_beacon_proposer_duty_canonical_beacon_proposer_duty.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.canonical_beacon_proposer_duty) FINAL
      expected:
        count: 21
