model: libp2p_gossipsub_beacon_attestation
network: mainnet
external_data:
    libp2p_gossipsub_beacon_attestation:
        url: https://data.ethpandaops.io/xatu-cbt/tests/mainnet/libp2p_gossipsub_beacon_attestation_libp2p_gossipsub_beacon_attestation.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.libp2p_gossipsub_beacon_attestation) FINAL
      expected:
        count: 4085506
