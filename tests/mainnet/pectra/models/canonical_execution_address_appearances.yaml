model: canonical_execution_address_appearances
network: mainnet
spec: pectra
external_data:
    canonical_execution_address_appearances:
        url: https://data.ethpandaops.io/xatu-cbt/pectra/canonical_execution_address_appearances.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.canonical_execution_address_appearances) FINAL
      expected:
        count: 74445
