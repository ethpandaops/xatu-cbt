model: canonical_execution_transaction
network: mainnet
spec: pectra
external_data:
    canonical_execution_transaction:
        url: https://data.ethpandaops.io/xatu-cbt/pectra/canonical_execution_transaction.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.canonical_execution_transaction) FINAL
      expected:
        count: 4139
