model: libp2p_synthetic_heartbeat
network: mainnet
spec: pectra
external_data:
    libp2p_synthetic_heartbeat:
        url: https://data.ethpandaops.io/xatu-cbt/pectra/libp2p_synthetic_heartbeat.parquet
        network_column: meta_network_name
assertions:
    - name: Has valid bounds
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          admin_cbt_external FINAL
        WHERE
          table = 'libp2p_synthetic_heartbeat'
      expected:
        count: 1
