- name: Expected bounds and size
  sql: |
    SELECT
      COUNT(*) AS count,
      MIN(slot) AS min,
      MAX(slot) AS max
    FROM
      fct_mev_bid_count_by_relay FINAL
  expected:
    count: 7
    min: 8815935
    max: 8815949
- name: Expected admin bounds
  sql: |
    SELECT
      fromUnixTimestamp(MAX(position + interval)) AS max,
      fromUnixTimestamp(MIN(position)) AS min
    FROM
      admin_cbt_incremental FINAL
    WHERE
      table = 'fct_mev_bid_count_by_relay'
  expected:
    max: '2025-10-27T00:29:48Z'
    min: '"2025-10-27T00:27:00Z"'
