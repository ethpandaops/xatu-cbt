model: canonical_execution_address_appearances
network: sepolia
spec: fusaka
external_data:
    canonical_execution_address_appearances:
        url: https://data.ethpandaops.io/xatu-cbt/sepolia/fusaka/canonical_execution_address_appearances.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.canonical_execution_address_appearances)
      expected:
        count: 71098
