model: canonical_execution_balance_reads
network: sepolia
spec: fusaka
external_data:
    canonical_execution_balance_reads:
        url: https://data.ethpandaops.io/xatu-cbt/sepolia/fusaka/canonical_execution_balance_reads.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.canonical_execution_balance_reads)
      expected:
        count: 13361
