model: canonical_execution_traces
network: sepolia
spec: fusaka
external_data:
    canonical_execution_traces:
        url: https://data.ethpandaops.io/xatu-cbt/sepolia/fusaka/canonical_execution_traces.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.canonical_execution_traces)
      expected:
        count: 25974
