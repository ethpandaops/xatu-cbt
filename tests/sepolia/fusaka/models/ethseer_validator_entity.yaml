model: ethseer_validator_entity
network: sepolia
spec: fusaka
external_data:
    ethseer_validator_entity:
        url: https://data.ethpandaops.io/xatu-cbt/sepolia/fusaka/ethseer_validator_entity.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.ethseer_validator_entity)
      expected:
        count: 1973
