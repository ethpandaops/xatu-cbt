model: fct_block_blob_count
network: sepolia
spec: fusaka
external_data:
    canonical_beacon_blob_sidecar:
        url: https://data.ethpandaops.io/xatu-cbt/sepolia/fusaka/canonical_beacon_blob_sidecar.parquet
        network_column: meta_network_name
assertions:
    - name: Contains orphaned block
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          fct_block_blob_count FINAL
        WHERE
          status = 'orphaned'
      expected:
        count: 20
    - name: Expected bounds and size
      sql: |
        SELECT
          COUNT(*) AS count,
          MIN(slot) AS min,
          MAX(slot) AS max
        FROM
          fct_block_blob_count FINAL
      expected:
        count: 40
        max: 8815952
        min: 8815933
    - name: Expected admin bounds
      sql: |
        SELECT
          fromUnixTimestamp(MAX(position + interval)) AS max,
          fromUnixTimestamp(MIN(position)) AS min
        FROM
          admin_cbt_incremental FINAL
        WHERE
          table = 'fct_block_blob_count'
      expected:
        max: "2025-10-27T00:30:24Z"
        min: "2025-10-27T00:26:36Z"
