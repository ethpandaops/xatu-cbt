model: fct_reorg_by_daily
network: sepolia
spec: fusaka
external_data:
    beacon_api_eth_v1_events_block:
        url: https://data.ethpandaops.io/xatu-cbt/sepolia/fusaka/fct_reorg_by_daily_beacon_api_eth_v1_events_block.parquet
        network_column: meta_network_name
    beacon_api_eth_v1_events_block_gossip:
        url: https://data.ethpandaops.io/xatu-cbt/sepolia/fusaka/fct_reorg_by_daily_beacon_api_eth_v1_events_block_gossip.parquet
        network_column: meta_network_name
    beacon_api_eth_v1_proposer_duty:
        url: https://data.ethpandaops.io/xatu-cbt/sepolia/fusaka/fct_reorg_by_daily_beacon_api_eth_v1_proposer_duty.parquet
        network_column: meta_network_name
    canonical_beacon_block:
        url: https://data.ethpandaops.io/xatu-cbt/sepolia/fusaka/fct_reorg_by_daily_canonical_beacon_block.parquet
        network_column: meta_network_name
    canonical_beacon_proposer_duty:
        url: https://data.ethpandaops.io/xatu-cbt/sepolia/fusaka/fct_reorg_by_daily_canonical_beacon_proposer_duty.parquet
        network_column: meta_network_name
    libp2p_gossipsub_beacon_block:
        url: https://data.ethpandaops.io/xatu-cbt/sepolia/fusaka/fct_reorg_by_daily_libp2p_gossipsub_beacon_block.parquet
        network_column: meta_network_name
assertions:
    - name: Row count should be greater than zero
      sql: |
        SELECT COUNT(*) AS count FROM fct_reorg_by_daily FINAL
      assertions:
        - type: greater_than
          column: count
          value: 0
    - name: Depth should be at least 1
      sql: |
        SELECT MIN(depth) AS min_depth FROM fct_reorg_by_daily FINAL
      assertions:
        - type: greater_than_or_equal
          column: min_depth
          value: 1
    - name: Reorg count should be greater than zero for all rows
      sql: |
        SELECT MIN(reorg_count) AS min_reorg_count FROM fct_reorg_by_daily FINAL
      assertions:
        - type: greater_than
          column: min_reorg_count
          value: 0
    - name: No null day_start_date values
      sql: |
        SELECT COUNT(*) AS null_count FROM fct_reorg_by_daily FINAL WHERE day_start_date IS NULL OR day_start_date = '1970-01-01'
      assertions:
        - type: equals
          column: null_count
          value: 0
    - name: No null updated_date_time values
      sql: |
        SELECT COUNT(*) AS null_count FROM fct_reorg_by_daily FINAL WHERE updated_date_time IS NULL OR updated_date_time = toDateTime('1970-01-01 00:00:00')
      assertions:
        - type: equals
          column: null_count
          value: 0
    - name: No duplicate day and depth combinations
      sql: |
        SELECT COUNT(*) AS duplicate_count FROM (SELECT day_start_date, depth, COUNT(*) AS cnt FROM fct_reorg_by_daily FINAL GROUP BY day_start_date, depth HAVING cnt > 1)
      assertions:
        - type: equals
          column: duplicate_count
          value: 0
    - name: Depth should be a reasonable value
      sql: |
        SELECT MAX(depth) AS max_depth FROM fct_reorg_by_daily FINAL
      assertions:
        - type: less_than
          column: max_depth
          value: 1000
    - name: Total reorg count should equal sum of individual reorg counts per day
      sql: |
        SELECT COUNT(DISTINCT day_start_date) AS day_count FROM fct_reorg_by_daily FINAL
      assertions:
        - type: greater_than
          column: day_count
          value: 0
