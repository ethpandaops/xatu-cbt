model: canonical_beacon_blob_sidecar
network: sepolia
external_data:
    canonical_beacon_blob_sidecar:
        url: https://data.ethpandaops.io/xatu-cbt/tests/sepolia/canonical_beacon_blob_sidecar.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.canonical_beacon_blob_sidecar)
      expected:
        count: 91
