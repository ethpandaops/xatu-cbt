model: canonical_beacon_proposer_duty
network: sepolia
external_data:
    canonical_beacon_proposer_duty:
        url: https://data.ethpandaops.io/xatu-cbt/tests/sepolia/canonical_beacon_proposer_duty.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.canonical_beacon_proposer_duty)
      expected:
        count: 21
