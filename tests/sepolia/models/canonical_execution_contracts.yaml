model: canonical_execution_contracts
network: sepolia
external_data:
    canonical_execution_contracts:
        url: https://data.ethpandaops.io/xatu-cbt/tests/sepolia/canonical_execution_contracts.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.canonical_execution_contracts)
      expected:
        count: 2860
