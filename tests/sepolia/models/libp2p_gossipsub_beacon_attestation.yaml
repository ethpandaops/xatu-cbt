model: libp2p_gossipsub_beacon_attestation
network: sepolia
external_data:
    libp2p_gossipsub_beacon_attestation:
        url: https://data.ethpandaops.io/xatu-cbt/tests/sepolia/libp2p_gossipsub_beacon_attestation.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.libp2p_gossipsub_beacon_attestation)
      expected:
        count: 4142
