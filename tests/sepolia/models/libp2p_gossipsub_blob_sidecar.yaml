model: libp2p_gossipsub_blob_sidecar
network: sepolia
external_data:
    libp2p_gossipsub_blob_sidecar:
        url: https://data.ethpandaops.io/xatu-cbt/tests/sepolia/libp2p_gossipsub_blob_sidecar.parquet
        network_column: meta_network_name
assertions:
    - name: total count
      sql: |
        SELECT
          COUNT(*) AS count
        FROM
          cluster('{remote_cluster}', default.libp2p_gossipsub_blob_sidecar)
      expected:
        count: 0
